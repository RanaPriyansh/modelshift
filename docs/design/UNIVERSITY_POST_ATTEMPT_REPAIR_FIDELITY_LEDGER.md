# University post-attempt repair fidelity ledger

**Record class:** unsigned local engineering evidence

**Recorded:** 2026-07-31

**Target implementation:** `bf90d9c1002c091d4ffea70f6e43548bcf35535d`

**Target tree:** `a927de9b1572b6cd59dc932e5b2b3155c56ea61a`

**Route:** `/internal/university-post-attempt-repair`

**Exact development token:**
`FORGE_UNIVERSITY_POST_ATTEMPT_REPAIR_FIXTURE=forge-university-post-attempt-repair.v1`

**Design basis:** the existing FORGE author shell, paper/ink/cyan/amber
tokens, editorial typography, mono evidence labels, native controls, visible
authority ceiling, and evidence-before-action hierarchy. The generated
concept image was a design input only. This is a removable internal research
surface, not a public learner route, dashboard, chat shell, answer generator,
or durable learning session.

**Claim ceiling:** this record binds an exact local source tree,
deterministic synthetic contracts, automated checks, a clean optimized build,
public-bundle scanning, and connected-Chrome desktop observations. It is not a
signed attestation, deployment receipt, live-data validation, learner/session
continuity proof, diagnosis, reviewed repair-policy approval, accessibility
conformance result, participant finding, learning result, or efficacy claim.

## Five-point fidelity ledger

| Point | Intended behavior | Bound evidence | Disposition |
| --- | --- | --- | --- |
| 1. Product grounding | Extend FORGE's editorial learning system without adding a generic AI dashboard, chat transcript, hidden score, grade, rank, streak, risk label, or recommendation. | The route reuses `ForgeShell`, existing type and color tokens, a bounded 1,240 CSS pixel reading surface, native radios/details/button, and the existing evidence and authority vocabulary. | Bound in source and desktop screenshot comparison |
| 2. Evidence before repair | Explain the exact immediate result before showing one next cognitive move. | DOM order is synthetic boundary, closed scenario control, result, server-paired context, deterministic evidence, repair/refusal, then authority ceiling. The mapped state shows one held and one open authored check before the repair instruction. | Bound in component tests and connected Chrome |
| 3. Exact mapping or visible stop | Select at most one repair through an exact fixed internal authored mapping; never infer a diagnosis or invent fallback advice. | The projector admits only an exact process-attested receipt, canonical World/runtime/package identity, and exact code/outcome/disposition/ordered criteria. Authentic alternate-partial and wrong-World receipts exercise `repair_mapping_missing` and `binding.mismatch`. | Bound in domain tests and independent contract/security review |
| 4. Native, stable interaction | Preserve native radio focus, one concise live update, reset continuity, and refresh-clear client state without moving a visible control. | Arrow Right moved checked state and focus together from the first to second scenario with zero observed desktop scroll delta. Reset restored the first radio, focus, heading, disabled state, and one status region. No app alert, false current-page navigation, or mobile navigation was present. | Bound in component tests and connected Chrome desktop |
| 5. Truth and authority | Keep the result immediate, synthetic, unsaved, non-diagnostic, non-grade, non-mastery, and unable to start repair/proof or change a path. | Context is visibly labelled server-paired synthetic and `receiptContextBinding` is `not_established`. The fixed mapping declares no independent/domain-review provenance. The client receives no raw Today request, World, receipt, answer criteria, learner prose, runtime/package digest, projector, or command. | Bound in contracts, scanner, tests, and production denial |

## Exact state matrix

| Closed scenario | Projector status | Evidence exposed | Repair behavior |
| --- | --- | --- | --- |
| One check open | `repair_ready` | One held and one open authored check | One fixed internal authored move is inspectable; nothing starts or saves |
| Two checks open | `repair_mapping_missing` | Bounded result count only; check-level repair detail withheld | Visible stop; no generic repair is invented |
| Both checks held | `not_applicable` | Two immediate checks held | No immediate repair; retention and broader capability stay untested |
| Receipt unavailable | `invalid` | None | Result and repair remain hidden |

The browser receives four deeply frozen presentation records. It does not
receive the raw Today request, World package, runtime receipt, attempt ID,
timestamp, answer criteria, learner prose, runtime/package digest, validator
input, policy selector, projector, command, or effect authority.

## Requirement-to-evidence traceability

Test aliases:

- `D`:
  `src/forge/university-post-attempt-repair/university-post-attempt-repair.test.ts`
- `C`:
  `src/components/forge/university/UniversityPostAttemptRepairWorkspace.test.tsx`
- `G`:
  `src/components/forge/university/UniversityPostAttemptRepairGate.test.ts`
- `A`:
  `src/components/forge/university/university-post-attempt-repair-public-artifact-boundary.test.ts`
- `E`: `tests/e2e/university-post-attempt-repair.spec.ts`
- `P`: `tests/e2e/university-post-attempt-repair-production.spec.ts`

| Requirement | Exact implementation and automated evidence | Current disposition and open gate |
| --- | --- | --- |
| `UV1-AFTER-001..002` | Exact canonical World identity plus process-local receipt attestation; authentic wrong-World, reconstructed, edited, missing, hostile-Proxy, and revoked-Proxy receipt refusals in `D`. | Mechanically met for the closed fixture. Durable event authority and exact learner/session/context continuity remain absent. |
| `UV1-AFTER-003..004` | Immutable fixed internal policy; exact code/outcome/disposition/ordered-criteria match; one neutral error class, operation, illustrative non-input response shape, and completion condition. | Implemented research candidate. The mapping has no independent or domain-review provenance and cannot support live use. |
| `UV1-AFTER-005..007` | Repair/proof boundaries, pass/unmapped/invalid distinctions, optional mapping rationale, and no accept/reject/rescore control in `D` and `C`. | Structurally met. Participant comprehension, control, and emotional-safety evidence remain open. |
| `UV1-AFTER-008..009` | Every consequential authority flag is false; frozen client fixture excludes raw requests, receipt, criteria across all scenarios, learner prose, digests, and internal identities in `D` and `C`. | Closed presentation boundary only. Identity, tenancy, rights, persistence, deletion/export, and incident operations remain open. |
| `UV1-AFTER-010` | Receipt attestation precedes Today traversal; a bounded detached snapshot uses Node's intrinsic Proxy detector before reflective traversal. Nested object/array, outer, receipt, revoked, accessor, symbol, exotic-prototype, and fail-closed tests pass in `D`. | Adequate for the in-process server fixture. This is not a remote request-body boundary; serialized-byte/string limits and pre-materialization key limits remain promotion work. |
| `UV1-AFTER-011` | Exact descriptor-safe development gate, production-only unavailable import path, repair-specific marker scan, aggregate split-chunk lexical scan, and incomplete-set negative control in `G` and `A`. | Exact clean build and local production denial pass. No deployment or provider-bound receipt follows. |
| `UV1-AFTER-012..013` | Fixed UX protocol, native controls, single live region, no current-page/mobile navigation, explicit synthetic context and non-binding copy in `C`; `E` and `P` define browser regressions. | Desktop connected-Chrome evidence is bound. Exact 320, real reduced-motion/forced-colors, zoom, and manual assistive technology remain open in the accepted evidence set. |

## Exact local verification

Target implementation checks:

- full primary Vitest: **145 files, 1,336 tests passed**;
- offline evaluator Vitest: **2 files, 13 tests passed**;
- total automated tests: **1,349 passed**;
- focused slice Vitest: **4 files, 30 tests passed**;
- full ESLint with zero warnings: passed;
- TypeScript: passed;
- `git diff --check`: passed;
- independent contract, security, and UI source/test reviews: no remaining
  P0–P2 or material P3 blocker.

Exact clean optimized build:

| Receipt field | Exact local value |
| --- | --- |
| Source commit | `bf90d9c1002c091d4ffea70f6e43548bcf35535d` |
| Source tree | `a927de9b1572b6cd59dc932e5b2b3155c56ea61a` |
| Source state | `clean` |
| Build ID | `forge-source-v1-bf90d9c1002c091d4ffea70f6e43548bcf35535d` |
| Artifact files | `1,414` |
| Artifact digest | `sha256:65120b260715bf4dfbd396331382175a6b3e6b6c99f8abe6b5a4cef4febf7855` |
| Public static assets scanned | `72` |
| Public asset digest | `sha256:12a62031acff2c609fa37e47f39107d5c33da40c7ce1a13fa5b1e90efb91f265` |
| Public directory | `5` files, `sha256:e0096e369f47666ca5a3f962b71b6f5199a17117ac5ce4a598d1b77dc42abac9` |
| Runtime configuration | `4` files, `sha256:17537b57dd805b15b29c12b6bb83019c61d19350d642fbb8255b77f2553aeceb` |
| Runtime cache policy | `fresh_ephemeral_next_cache_v1` |

These are unsigned local receipts. They do not establish pushed-source
identity, provider-bound provenance, immutable deployment, rollback
readiness, monitoring, or production operation.

## Connected Chrome observations

The target implementation was served from its exact development token on the
server's canonical `localhost` origin.

- all four radios had exact accessible names and visible native indicators;
- the default mapped state placed deterministic evidence before one repair;
- Arrow Right moved focus and checked state to the no-mapping scenario;
- the H1 changed to `Stop before inventing advice.`;
- one app-owned polite status region and zero app-owned alert regions existed;
- `aria-current="page"` and mobile navigation were absent;
- selection caused a `0` CSS pixel desktop scroll delta;
- Reset restored the first checked/focused radio, original H1, one status
  region, and disabled Reset;
- the illustrative response shape was labelled as not an input and no textbox
  or start/save/submit/retry/rescore/apply/accept action existed;
- the final boundary used non-text borders, and generated slash text was
  absent from the accessible article text.

The connected Chrome viewport-control capability did not change the main
agent's CSS viewport from 2,000 pixels to 320 pixels. Exact 320 behavior is
therefore an open main-agent browser gate even though the source/test audit
found no remaining mobile blocker.

Development Chrome reported a hydration mismatch naming the installed Dark
Reader attribute `data-darkreader-proxy-injected`. This is extension
interference before React hydration, so the affected full-page capture was
not accepted as clean console evidence. The interaction and DOM checks still
worked after hydration.

The exact clean production build was then started locally with the development
token still configured. Connected Chrome rendered only:

`Post-attempt repair is unavailable.`

It exposed zero main radios, zero main buttons, none of the development title,
repair, or activity copy, one explicit main alert, no current-page navigation,
and no production-scoped console log or error.

This is local production-mode denial evidence, not deployment or live
production evidence.

## Excluded and unexecuted evidence

`E` defines native keyboard behavior, all four 320-pixel transitions and
reset, scroll stability, no overflow, reduced motion, forced colors, native
radio visibility, and layout stability. `P` defines production denial.

The main agent did not authorize or execute Playwright because the selected
Product Design browser was the user's connected Chrome. A delegated audit
invoked a Playwright lane outside that selected-browser constraint; its output
is explicitly excluded from this ledger and from all acceptance claims.

## Evidence still required

Before any accessibility, demand, learning, live-product, or production claim:

1. reproduce exact 320 CSS pixel interactions in the selected connected
   browser and retain clean viewport evidence;
2. exercise actual reduced-motion and forced-colors preferences, zoom, and
   manual screen-reader/assistive-technology review with an authorized
   reviewer;
3. run the approved adult observation protocol before making comprehension,
   autonomy, emotional-safety, substitution, or demand conclusions;
4. obtain independent/domain review provenance for every eligible repair
   mapping before presenting it as reviewed content;
5. establish authenticated adult identity, tenant/data rights, exact
   learner/session/course/path continuity, durable tamper-resistant
   receipt/event authority, retention, export/correction/deletion, incident,
   and appeal operations;
6. create fresh independent proof task families and validate the
   support-withdrawal boundary with participants;
7. require pushed-source identity, provider-bound immutable build, deployment
   verification, monitoring, rollback authority, and release approval before
   changing the production claim.

The generated concept image and local screenshots remain design inputs and
engineering evidence only. They are not participant or production evidence.
