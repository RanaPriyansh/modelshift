# University recovery what-if fidelity ledger

**Record class:** unsigned local engineering evidence

**Recorded:** 2026-07-31

**Target implementation:** `348e3125556d037ccce3dccd5132d996cabcd50c`

**Target tree:** `5d1fcd863fe491b9df9ad72e00f76a2454281360`

**Route:** `/internal/university-recovery`

**Exact development token:**
`FORGE_UNIVERSITY_RECOVERY_FIXTURE=forge-university-recovery-what-if.v1`

**Design basis:** the existing FORGE author shell, paper/ink/cyan/violet
tokens, editorial typography, mono evidence labels, native controls, source
uncertainty vocabulary, and visible authority ceiling. This is a transient
single-variable Recovery experiment inside the existing internal research
route. It is not a dashboard, chat shell, arbitrary planner, or public
learner route.

**Claim ceiling:** this record binds an exact local source tree, deterministic
synthetic contracts, automated checks, a clean optimized build, public-bundle
scanning, and connected-Chrome observations. It is not a signed attestation,
deployment receipt, live-data validation, identity or tenancy proof,
accessibility conformance result, participant finding, product-demand result,
learning result, or efficacy claim.

The evidence-binding change that adds this ledger also names an explicit
finite unsafe-integer regression vector. That test-only change does not alter
the target implementation or browser surface. Evidence about the target is
kept distinct from the commit that records it.

## Five-point fidelity ledger

| Point | Intended behavior | Bound evidence | Disposition |
| --- | --- | --- | --- |
| 1. Product grounding | Extend FORGE's existing editorial learning system without adding a generic AI dashboard, chat transcript, invented score, rank, streak, risk label, or recommendation. | The surface reuses `ForgeShell`, existing tokens and typography, mono evidence labels, native radios, a bounded 1,240 CSS pixel reading surface, and the established source/Recovery/authority vocabulary. No new image, icon, chart, score, rank, risk, streak, or recommendation component was added. | Bound in source; screenshot comparison open |
| 2. Evidence before choice | Show exactly what remains fixed before asking the learner to vary available time. | DOM order is preview boundary, question, fixed evidence, neutral choices, one selected consequence, copied-source boundary, then authority ceiling. The fixed evidence shows the copied deadline, full 90–120 minute effort range, protected 30 minute buffer, learner classification, and the item held outside the window. No option is selected initially. | Bound in source, component tests, and connected Chrome |
| 3. One-variable consequence | Change only `recoveryWindow.availableMinutes`, recompute through canonical Recovery, and show exact arithmetic without choosing for the learner. | The strict request schema admits no supplied result, lane, status, projection, command, or effect. Locked-field binding covers the source and identity envelope, dates, buffer, items, decisions, effort, dispositions, dependencies, routes, and IDs. The three server-precomputed choices reproduce direct canonical Recovery projections and digests. | Bound in domain tests and independent contract/security review |
| 4. Native, stable interaction | Preserve native radio focus, keep results refresh-clear, expose one concise status update, and prevent result expansion or contraction from moving an already-visible control. | At exact 320 CSS pixel width, the second and third radio cards were both fully visible before the transition. Arrow Right changed 130 → 100 minutes and inserted prepared-human-help copy with a `0` CSS pixel scroll delta; Arrow Left removed that copy with a `0` CSS pixel delta. Focus and checked state moved together. Reset cleared all radios, removed the result, emptied the status region, disabled itself, and focused the first radio. The guard does not override native scrolling when the changed control is clipped or offscreen. | Bound in component tests and exact-tree connected Chrome |
| 5. Truth and authority | Keep the work synthetic, preview-only, unsaved, unrecommended, and unsent; never imply that copied data is official or complete. | The opening boundary says `Fixed synthetic adult fixture`, `Preview only`, `Not saved`, `Not recommended`, and `Nothing sent`. Source copy says review does not establish authenticity or institutional completeness. The human-help outcome is prepared but has no send control. The footer denies recommendation, plan/calendar, persistence, messaging, and external-effect authority. | Bound in contracts, fixture DTO, component tests, browser DOM, and production denial |

## Exact consequence matrix

| Selected available time | Protected buffer | Workable time | Canonical Recovery consequence | Visible treatment |
| --- | --- | --- | --- | --- |
| 240 minutes | 30 minutes | 210 minutes | `draft_ready` | The full unchanged 90–120 minute effort range fits |
| 130 minutes | 30 minutes | 100 minutes | `learner_choice_required` | Only the low estimate fits; learner choice remains |
| 100 minutes | 30 minutes | 70 minutes | `human_help_required` | Even the low estimate does not fit; a question is prepared but not sent |

The browser receives three closed presentation records. It does not receive a
raw Recovery request, owner or tenant identity, source identity, candidate or
decision identity, projection digest, arbitrary input, projector, command, or
effect authority.

## Requirement-to-evidence traceability

Test aliases used below:

- `D`:
  `src/forge/university-recovery-what-if/university-recovery-what-if.test.ts`
- `S`: `src/forge/bounded-json-snapshot.test.ts`
- `C`:
  `src/components/forge/university/UniversityRecoveryWhatIfWorkspace.test.tsx`
- `G`: `src/components/forge/university/UniversityRecoveryGate.test.ts`
- `A`:
  `src/components/forge/university/university-recovery-public-artifact-boundary.test.ts`
- `E`: `tests/e2e/university-recovery-what-if.spec.ts`
- `P`: `tests/e2e/university-recovery-production.spec.ts`

| Requirement | Exact implementation and automated evidence | Current disposition and open gate |
| --- | --- | --- |
| `UV1-RECOVERY-WHATIF-001` | ADR-020 and the UX experiment bind one atomic preview job and exact synthetic base request. `C::shows fixed evidence before neutral choices with no default result`; `E::keeps evidence before neutral choices and preserves native keyboard focus`. | Implemented synthetic candidate. Approved adult observation, substitution/demand evidence, and `UV1-GATE-001..003` remain open. |
| `UV1-RECOVERY-WHATIF-002` | Strict request parsing plus `projectUniversityRecoveryWhatIf` snapshots the raw boundary and calls canonical `projectUniversityRecovery`. `D::recomputes the canonical ...` for all three cases; `D::makes the baseline selection exactly equal to direct canonical Recovery`; `D::accepts no supplied status, projection, command, or effect field`; `D::does not mutate the caller's raw Recovery request`. | Mechanically met for the deterministic fixture. Live request identity and source authority remain outside scope. |
| `UV1-RECOVERY-WHATIF-003` | `lockedRequest`, `lockedFieldsDigest`, and `adjustedRequest` replace only available minutes. `D::keeps every field except available minutes locked`; the three canonical matrix cases; raw-input-unchanged coverage. | Canonical parsed fields are bound. Participant understanding that only one declaration changed is unverified. |
| `UV1-RECOVERY-WHATIF-004` | Source review returns `source_review_required` with null selection/capacity; the fixture emits no choices; the component removes the fieldset and result. `D::withholds selection and capacity when the copied source needs review`; `C::removes every capacity control when source review is required`. | Domain and component branches pass. The route exposes only the reviewed sample; a rendered source-review harness remains optional evidence work, not a production capability. |
| `UV1-RECOVERY-WHATIF-005` | Canonical capacity produces exact 240−30=210, 130−30=100, and 100−30=70 arithmetic while preserving 90–120 minutes. Three domain matrix cases; `D::fails closed for invalid capacity instead of repairing the protected buffer`; three `C::renders transparent arithmetic ...` cases. | Mechanically met. Adult reconstruction/comprehension and manual assistive-technology reading remain open. |
| `UV1-RECOVERY-WHATIF-006` | Every consequence comes from canonical Recovery; authority denies recommendation; visible labels are neutral values. Three full child-projection equality cases; the no-default component case. | No recommendation field or claim exists. Emotional-safety and consequence-calibration observations remain open. |
| `UV1-RECOVERY-WHATIF-007` | Selection is local React state; reset clears it; no apply/save/history/calendar/reminder/sync control exists. `C::retains native radio focus and reset ...`; `C::keeps native focus and restores a visible control ...`; `C::does not override native scrolling ...`; `C::performs no network, storage, history, clipboard, or navigation effect`; exact-tree Chrome expansion/contraction/reset evidence. | Transient interaction is locally bound. A browser refresh-clear assertion exists as an E2E opportunity but the E2E file was not executed in this pass. |
| `UV1-RECOVERY-WHATIF-008` | Frozen fixture-only authority, visible synthetic boundary, copied-source disclaimer, and three `Not allowed` operational definitions. Authority assertions in every domain matrix case; frozen DTO and no-effect component cases. | Fixture-only ceiling is met. Identity, tenancy/RLS, rights, source authenticity, completeness, real-data authority, and responsible-human operation remain open. |
| `UV1-RECOVERY-WHATIF-009` | Shared bounded snapshot rejects accessors, hidden fields, proxies before traps, exotic prototypes, cycles, depth/node/key/array overflow, nonfinite numbers, and an explicit finite unsafe integer. Strict Zod schemas, canonical digest, deep freeze, and no effect-capable dependency complete the boundary. `D` hostile/determinism/no-effect cases and all `S` boundary cases. | Materially covered for local pure computation. This is not live provider, database, or operational evidence. |
| `UV1-RECOVERY-WHATIF-010` | Projector and fixture are server-only; development composition precomputes a minimal frozen presentation DTO. `C::receives a frozen presentation fixture without raw requests or internal identities`; `A` marker and complete lexical-set tests. | Closed browser DTO and exact build scan pass. No raw request or server-only marker was promoted to a public asset. |
| `UV1-RECOVERY-WHATIF-011` | Exact descriptor/proxy-safe development gate; production imports only the unavailable shell; dedicated `test:e2e:university-recovery-what-if` and legacy Recovery scripts use separate exact tokens; `G` gate tests; all `A` tests; four `E` definitions; `P` denial definition. | Exact local build and connected-Chrome production denial pass. Playwright definitions were not executed, actual media preferences/manual AT are open, and local production is not deployment evidence. |

## Exact local verification

Target implementation checks:

- full primary Vitest: **141 files, 1,306 tests passed**;
- offline evaluator Vitest: **2 files, 13 tests passed**;
- full ESLint with zero warnings: passed;
- TypeScript: passed;
- `git diff --check`: passed;
- independent contract, UI, and security reviews: no remaining blocker.

Exact clean optimized build:

| Receipt field | Exact local value |
| --- | --- |
| Source commit | `348e3125556d037ccce3dccd5132d996cabcd50c` |
| Source tree | `5d1fcd863fe491b9df9ad72e00f76a2454281360` |
| Source state | `clean` |
| Build ID | `forge-source-v1-348e3125556d037ccce3dccd5132d996cabcd50c` |
| Artifact files | `1,398` |
| Artifact digest | `sha256:6a6582af40f250f8023f79db9809f52b982a0ca0a78cf3dcdb7f552e54273673` |
| Public static assets scanned | `71` |
| Public asset digest | `sha256:d787e29b7395f6964d9a124777b325d43727eaf8c860aaa16e06b72f370c7584` |
| Public directory | `5` files, `sha256:e0096e369f47666ca5a3f962b71b6f5199a17117ac5ce4a598d1b77dc42abac9` |
| Runtime configuration | `4` files, `sha256:2ac0050210914a835dd399b64d80c321cf8203a5d222d79654983a2621ea02f5` |
| Runtime cache policy | `fresh_ephemeral_next_cache_v1` |

These are unsigned local receipts. They do not establish pushed-source
identity, provider-bound provenance, immutable deployment, rollback
readiness, or production operation.

## Connected Chrome observations

The exact target tree was served with the exact development token.

Desktop:

- viewport was 2,000 by 1,127 CSS pixels;
- document and body widths were exactly 2,000 with no horizontal overflow;
- the surface was centered at `x=380`, measured 1,240 CSS pixels wide, and
  retained the existing author-shell navigation;
- the main heading measured 920 by 157.4 CSS pixels;
- the three neutral radio cards were one row, each 406.7 by 76 CSS pixels;
- no option was selected, one status region existed, and no navigation item
  claimed `aria-current="page"`.

At exact 320 CSS pixel width:

- viewport was 320 by 744; document and body widths were exactly 320;
- the article measured 288 CSS pixels wide at `x=16`;
- the main heading measured 288 by 120.9 CSS pixels;
- all three radio cards measured 288 by 68 CSS pixels;
- the second and third controls were fully inside the viewport before
  keyboard selection;
- expansion into the prepared-human-help result and contraction back to the
  learner-choice result each had a `0` CSS pixel scroll delta;
- checked state and focus moved together;
- reset left three unchecked radios, no selected result, an empty status
  region, a disabled reset button, and focus on the first radio.

The development console reported one hydration mismatch that named the
installed Dark Reader attributes, including
`data-darkreader-proxy-injected`. No application-owned console error was
observed.

The exact clean production build was then started locally with the what-if
token still configured. At desktop and exact 320 CSS pixel width it rendered
only `University recovery is unavailable.`:

- zero radios and zero main buttons;
- no what-if question, `Argument analysis`, choice label, or internal result;
- the exact development-fixture boundary remained visible;
- document width matched the viewport at 320;
- no production-scoped console error was recorded.

This is local production-mode denial evidence, not a deployment or live
production claim.

## Evidence definitions not executed

`tests/e2e/university-recovery-what-if.spec.ts` now defines separate checks for
native keyboard behavior, exact 320 CSS pixel reflow and scroll stability,
reduced motion, forced colors, selected/unselected differentiation, focus
outline, and per-test console/page errors. The package script binds the exact
what-if token separately from legacy Recovery.

Those Playwright definitions were **not executed** because the selected
browser for this pass was the connected user Chrome, and direct Playwright
execution requires user permission under the active Product Design workflow.
Their presence is test preparation, not passing evidence.

## Evidence still required

Before any fidelity, accessibility, demand, learning, live-product, or
production claim:

1. capture exact-target desktop and 320 CSS pixel screenshots once the
   connected Chrome screenshot endpoint or macOS Screen Recording permission
   is available;
2. compare the reference/design input and implementation at the same viewport
   in one visual-review input;
3. run the actual reduced-motion and forced-colors preferences and retain
   rendered evidence;
4. complete keyboard plus manual screen-reader/assistive-technology review
   with an authorized reviewer;
5. run the approved adult observation protocol before making comprehension,
   autonomy, emotional-safety, substitution, or demand conclusions;
6. keep source authenticity, real course data, identity, tenant isolation,
   RLS, durable storage, provider operation, responsible-human operation,
   participant operation, and efficacy gates explicitly open;
7. require pushed-source identity, a provider-bound immutable build,
   deployment verification, monitoring, rollback authority, and release
   approval before changing the production claim.

The generated concept image remains a design input only. It is not evidence
for this exact implementation.
