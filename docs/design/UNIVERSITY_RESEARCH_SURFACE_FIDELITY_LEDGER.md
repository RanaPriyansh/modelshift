# University research surface fidelity ledger

**Target:** exact Candidate A
`9fb4d22142deec7c29f1c15a59d0dcc4b7d118c1`

**Design basis:** existing FORGE typography, paper/ink/cyan tokens, mono
evidence labels, bounded 1,280 CSS pixel reading surface, semester-loop
journey, and visible authority boundary. This is a refoundation of the
existing internal semester-loop surface, not a new visual brand.

**Fidelity ceiling:** source and browser measurements are bound; exact
same-viewport screenshot comparison is not complete because macOS Screen
Recording permission was unavailable. No visual-parity, accessibility
conformance, or human-equivalence claim follows.

## Five-point fidelity ledger

| Point | Intended behavior | Candidate A evidence | Disposition |
| --- | --- | --- | --- |
| 1. Product grounding | Preserve FORGE's restrained editorial hierarchy and existing design tokens while removing dashboard-like state density. | The candidate reuses the existing shell, token variables, journey vocabulary, mono labels, paper/ink palette, and 1,280 CSS pixel maximum. It adds no new palette, typeface, icon family, decorative asset, or generic chat pattern. | Bound in source; screenshot comparison open |
| 2. Information hierarchy | Make one bounded job dominant, then keep exact facts, choices, effects, tasks, and terminal limits separately inspectable. | One scenario is visible at a time. The order is hero, five-stage journey, current job, seven facts, choices/effects, nine fixed tasks, then terminal and digest limits. | Bound to the shared packet and browser DOM |
| 3. Core interaction | Let a learner compare all seven states without persistence, routing, or hidden client state. | Seven native radios use the browser's keyboard model. Arrow Right moved checked state and focus together. The only active job control is an exact same-document fragment; it focused the matching effect boundary. | Observed in connected Chrome for Pack P; all states observed for P and Q |
| 4. Responsive and access burden | Keep the complete comparison reachable at 320 CSS pixels, maintain 44 CSS pixel controls, avoid horizontal scrolling, and require no motion. | At 320 by 900, both packs showed exactly one matching state, controls measured 44 CSS pixels, and document width remained 320. The observed candidate had zero non-zero transition or animation durations. At 2,000 CSS pixels, the surface remained 1,280 wide and the selector stayed on one row. | Browser-bound; forced colors, screen reader, and manual assistive technology open |
| 5. Truth and evidence | Make synthetic, source, learner-choice, effect, and completion limits more visible than product confidence. | The boundary states synthetic-only, copied facts are not university truth, local navigation only, and no save/send/session/submission/evidence. The footer repeats the full claim ceiling. Production renders the unavailable state even with a fixture token. | Bound locally; no participant, live-data, deployment, or efficacy authority |

## Exact user-facing copy delta

These changes are intentional comparison-contract changes, not unmeasured
marketing rewrites.

| Surface area | Previous internal semester-loop copy | Candidate A copy |
| --- | --- | --- |
| Source boundary | `Copied sources are not university truth` | `Copied facts are not university truth` |
| Recommendation/effect boundary | `No recommendation` | `Local navigation only` |
| Side-effect boundary | `No save, session, message, or evidence` | `No save, send, session, submission, or evidence` |
| Selector legend | `Stress-test the same semester` | Retained exactly |
| Selector labels | `Ready`; `Source review`; `Capacity break`; `Tight window`; `World changed`; `Path complete`; `Path blocked` | `Protected Study Ready`; `Source Review Required`; `Recovery Required`; `Learner Choice Required`; `World Review Required`; `Path Complete`; `Path Blocked` |
| Primary headline | Seven scenario-specific action headlines | `One semester. One honest next move.` with the exact current state in the eyebrow |
| Evidence introduction | `Why this state exists` / `The checks stay separate.` | `Same canonical scenario record` / `The facts stay inspectable.` |
| Evidence body | Four hand-authored cards plus a separate recovery strip | Seven shared-packet facts, exact choices, bounded next job, no-effect table, nine tasks, and terminal limits |
| Primary control | Separate synthetic routes such as `Review copied sources` or `Inspect recovery draft` | Shared-packet local labels that navigate only to the same scenario's effect boundary |
| Authority footer | `Navigation explanation only` plus projection summary | Exact terminal entries, scenario/projection digest prefixes, and the full local synthetic claim ceiling |

The candidate-specific hierarchy and state labels are the intended treatment
difference. Exact facts, choices, bounded job, effects, tasks, and terminal
claims come from the same packet used by the neutral substitute and are not
separately authored in this component.

## Visual evidence still required

Before any fidelity or equivalence claim:

1. enable macOS Screen Recording for the browser-control host;
2. capture Candidate A and the neutral substitute at the same desktop and 320
   CSS pixel viewports and exact scenario;
3. place each source/candidate pair in one comparison input;
4. inspect cropping, padding, type, borders, focus, salience, reading order,
   density, and difficulty;
5. repeat for forced colors and complete manual assistive-technology review.

The existing pre-repair screenshots and the generated concept image are
design inputs only. They are not Candidate A evidence.
