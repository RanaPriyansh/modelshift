# FORGE Completion and Evidence Matrix

**Authority:** principal review task

**Purpose:** map the full FORGE objective to current evidence, missing proof, owners, and stop rules

**Current deployed code/source SHA:** `35a1d2c5bd28c7b794b26414c6bdfe8f18097027`

**Public alias:** `https://modelshift.vercel.app`
**Production deployment:** `dpl_Er7rVecXt3iga56P4uPDoLnWt9V4`
**Immutable deployment URL:** `https://forge-learning-7a63ywsp5-ranapriyanshs-projects.vercel.app`

This is a bounded current public record for `35a1d2c`. Public health binds the exact SHA, platform identity, canonical build time, lock/content/evaluator digests, and disabled cloud/provider posture. The release is `DEPLOYMENT_BLOCKED`, not `DEPLOYED_CANDIDATE` or terminal `PRODUCTION_VERIFIED`: the manifest asserted locally precomputed public-asset digest `b1f8d95f...`, while the exact Vercel remote build emitted `aad493295...`. Public verification passed 206 checks and failed four release-identity checks. The [canonical current record](../operations/CURRENT_RELEASE.md) owns the exact tuple and release-provenance/rollback gates. This documentation amendment is not itself deployed.

**Wave 6 code cut reviewed by this amendment:** `35a1d2c5bd28c7b794b26414c6bdfe8f18097027` contains the accepted authority preflight, frontend route/accessibility/planner repairs, deterministic non-routed adult-pilot controller kernel, and release-manifest/verifier foundation above the prior fixture/compiler and presentation chain. The kernel is not an assignment, route, provider, persistence, or evidence-authority path. The prior accepted Wave 5 baseline at `7ba70ef60c038883bd4ae9ca89c974d9bec61cea` retains its recorded evidence and limits. None of these additions creates trusted curriculum, persistent review, live provider, runtime assignment, authenticated identity, durable proof, minor operation, or homeschool authority.

**Audit date:** 23 July 2026

**Verdict:** the full objective is not complete

This file prevents a strong prototype, a broad plan, or a green test count from being mistaken for a production-grade lifelong or homeschool learning system. It remains current only when the principal updates it after reviewing integrated evidence on an exact SHA.

## 1. Disposition vocabulary

| Disposition | Meaning |
| --- | --- |
| `PROVEN` | Direct current-source or freshly executed evidence proves the exact bounded statement. It does not generalize beyond that statement. |
| `PARTIAL` | A real implementation/evidence slice exists, but a required condition, integration, population, mode, or proof level is absent. |
| `CONTRADICTED` | Current behavior or authoritative evidence conflicts with the requirement. |
| `MISSING` | No implementation or evidence satisfying the requirement exists on current `main`. |
| `UNVERIFIED` | An implementation or claim may exist, but current authoritative execution or external evidence is absent. |
| `IN_PROGRESS` | A bounded worker owns the next slice; nothing is accepted until principal review and integration. |
| `REQUEST_ONLY` | Founder direction, research, architecture, or a proposed plan exists, but no implementation or behavioral evidence is claimed. |

Documentation is never sufficient proof of behavior. Mocked provider/auth tests are not live provider/auth proof. A local browser ledger is not tamper-resistant independent evidence. A public URL is not a verified current release unless its source SHA and runtime state are known.

## 2. Engineering evidence and code-cut boundary

### 23 July 2026 source-corroboration presentation amendment

`39d86820e46fea6e7df4f4dadc48a19a5fa7e7eb` adds `/paths/source-corroboration`, an adult presentation-only fixture path. Its server-only projection accepts only the exact known source-corroboration runtime binding (`sha256:a172f067f6135bdcec13c66053ef250ef92692db734b60ddf8e396fb8b0dc4b5`), the existing released-capability availability for **Corroborate a model-generated factual claim** in the public AI & Learning World at `/learn/ai-and-learning`, the written-explanation project template `project-template.authored.written-explanation@1.0.0` (`sha256:bc29a41e7e74d78604e437805d63cbeaed79b53e05871a5d57d838bbf20c2869`), the non-physics foundation-rehearsal practice template `practice-template.authored.foundation-rehearsal@1.0.0` (`sha256:2e391c46140c10b6355e79eb014e9962e66cac1d4673b7c9c300b92c7dee7234`), and a disabled-provider plan (`no-iframe-request`, `provider-disabled`). Altered, malformed, or getter-backed presentation inputs fail closed.

The route is an ephemeral scratchpad, not a continuation service: the internal CTA goes only to the existing AI & Learning source-corroboration World; there is no iframe, external request, `localStorage` mutation, submission, evidence write, provider call, assignment, proof authority, or return operation. The World binding remains honour-based and `not_persisted`; the route adds no reviewed resource, real project assignment, durable evidence, adult entitlement, minor authority, or homeschool authority.

| Local gate on exact code cut `39d8682` | Recorded result | Boundary retained |
| --- | --- | --- |
| Application/evaluator and static gates | 75 files / 721 application tests and 2 files / 13 evaluator-contract tests PASS; lint, typecheck, and diff checks PASS | Coverage proves the exact local presentation and contracts, not live-provider operation, reviewed sources, learner research, or production operation. |
| Production build and public-boundary scan | PASS; the verifier enumerated 37 static assets | A local optimized build and static-asset boundary are not CI, deployment, a new public-release identity, or production operation. |
| Focused path and targeted rendered checks | Focused source-path tests 10/10 PASS; targeted desktop/mobile E2E 2/2 PASS | These checks cover the bounded fixture presentation, including refresh-clear scratchpad and local/no-external boundary, not a full browser matrix or representative-user/access evidence. |
| Full browser matrix | 74 pass / 22 intentional project-scoped skips / 0 fail across 96 cases in 2.3m | This exact-commit automated matrix is not manual assistive-technology, Safari, Firefox, representative-learner, learning-validity, or retention evidence. |

### 23 July 2026 practical-path planning and fixture implementation amendment

`docs/program/FOUNDER_IDEA_LOG.md`, `docs/program/AI_ERA_LEARNING_THESIS.md`, proposed `docs/adr/0009-practical-multimodal-learning-paths.md`, and proposed `docs/program/WAVE_6_PLAN.md` define a goal-to-capability-map direction, a governed external-resource lifecycle, a visual representation registry, practical projects, educator review, adult-first rollout, and 5/10/15/20-year scenarios. The research ledger is `RESEARCH_READY` for its five planning questions.

The architecture, adult-pilot proposal, educator mode, and future scenarios remain `REQUEST_ONLY`. The following bounded implementation now exists locally:

| Slice | Exact code | Current authority | Explicitly absent |
| --- | --- | --- | --- |
| Capability map | `e7f51101fc2ff273fee61d259eefb6d3af9219e0` | Pure fixture compiler and validator over caller-asserted, unverified reviewed-map inputs; assignment is disabled. | Trusted curriculum/source/reviewer identity, persistence, publication, runtime routing, learner entitlement, or proof authority. |
| Resource catalog and YouTube candidate | `50e55fecfb9e7c3cbdfd17ad77875c35ab23d206` | Fixture-only, content-addressed resource review/eligibility contracts and a disabled YouTube metadata adapter; unknown policy facts fail closed. | Network calls, credentials, search/feed/transcript/media/embed behavior, persistent review identity, live refresh/removal, ranking, assignment, or provider endorsement. |
| Representation registry | `9a17a32d0e37e7b7cf169de002cdb463654415f2` | Fixture registry validates exact representation/dependency/review closure and bounded deterministic authored frame alternatives. | Generated-visual authority, live runtime publication, broad domain coverage, representative access evidence, or proof/assignment authority. |
| Project and practice | `488e7210019c0cf38c303aca9775ea2b8a5a0c0f` plus timestamp repair `1c3a4c462de7749a062935735ddc76fc1e2c7533` | Fixture compiler revalidates exact map, project, representation, practice, critique/revision, provenance, proof, and return bindings; completion remains `complete-self-declared-unverified`. | Learner identity, durable artifacts, external validation, live materials/safety review, UI/runtime operation, scoring, mastery, or trusted evidence. |
| Exploratory map preview | `a0eb0758fa1ac029d0ee93c4ded3129fb802f368` | Unknown questions preserve the learner's exact words, show four source-verification gates, and accept an explicitly local, unsaved revision note. | Connection to the fixture compiler/catalog/registry/project contracts, submission, storage, reviewer workflow, published path, assignment, or learn-anything claim. |

These are real contract and interface slices, so MAP-01, RESOURCE-01, VISUAL-01, and PROJECT-01 are no longer wholly request-only. They remain `PARTIAL` and fixture/local-only. Wave 6's end-to-end adult journey, server entitlement, resource operation, educator review, protected proof and return, evaluation, production release, and every minor or homeschool gate remain incomplete.

| Local gate on exact code cut `a0eb075` | Recorded result | Boundary retained |
| --- | --- | --- |
| Application/evaluator and static gates | 73 files / 711 application tests and 2 files / 13 evaluator-contract tests PASS; lint, typecheck, and diff checks PASS | Fixture and evaluator-contract coverage is not live-model/provider evaluation, educational validity, learner research, or production operation. |
| Production build and public-boundary scan | PASS; the verifier enumerated 36 static assets | A local optimized build and static-asset boundary are not CI, a deployment, source/release identity, source-map absence proof beyond the verifier's checks, or production operation. |
| Full automated browser matrix | 72 pass / 22 intentional project-scoped skips / 0 fail | This covers the current routed browser surface, including the new preview's test path. It is not manual assistive-technology, Safari, Firefox, representative-learner, learning-validity, or retention evidence. |
| Disposable PostgreSQL harness | Fresh and legacy-upgrade fixtures plus the separate-session nonce race PASS; both `forge_w5c_*` databases were deleted and absence was verified | This is local disposable-database contract evidence only. It proves no hosted database, application persistence, configured auth/RLS operation, backup/restore, recovery, or production durability. |

### Wave 5 local amendment for `7ba70ef`

Wave 5 adds local trust and persistence contracts plus one internally retained Argument & Evidence package. The package implements the compiler, governed authored support, reconstruction, withdrawal, unfamiliar transfer, deterministic validator, and bounded receipt path. Its authored fixture remains internal and unavailable: it is not in the public catalog, planner, curriculum graph, route tree, or static asset surface.

| Gate | Recorded result | Boundary retained |
| --- | --- | --- |
| Application/evaluator | 63 files / 626 application tests and 2 files / 13 evaluator-contract tests PASS | Test volume is not learning validity, public-route operation, or durable authority. |
| Offline evaluation | 54 fixtures valid; clear-fixture baseline 29/38; authored probe PASS; live suite `NOT_RUN` | No live model/provider quality or privacy claim follows. |
| Static engineering | Typecheck, lint, diff checks, and production build PASS; 13 pages and 36 static assets | A successful build is not a deployment or publication decision. |
| Integrated browser | 70 pass / 22 intentional skips / 0 fail | This verifies the existing public surface; W5-E has no route. It is not W5-E browser/manual-AT evidence. |
| Retained boundary | `/` `200`; W5-E route/fixture/provenance and legacy ModelShift route `404`; no static W5-E markers or source maps | Absence proves retention, not curriculum/source/publication readiness. |
| Local SQL | Fresh and historical-upgrade fixtures plus 12 overlapping v1/v2 races PASS; both named disposable databases deleted; zero fixtures remain | No hosted database, RLS/auth operation, application append, backup/restore, recovery, or production durability follows. |

Independent W5-E reviewers accepted exact candidate `a38faa11a51c5f48d791c1f06770f70cf9bd3d3c` after the public-fixture, duplicated-domain-truth, attestation, clone-forgery, and mutable-binding findings were repaired. The conflict-free integrated code is `7ba70ef60c038883bd4ae9ca89c974d9bec61cea`; this documentation commit follows it and is not part of that code SHA.

### Historical Wave 4 amendment for `c7d4011`

Wave 4 migrates Force and Motion and AI & Learning into the shared runtime alongside Primary Source Reasoning and Proportional Reasoning. The accepted release records four authored bounded Worlds, not a general-purpose curriculum or education system.

| Gate | Recorded result | Boundary retained |
| --- | --- | --- |
| CI | pre-main `workflow_dispatch` run `29944088585` PASS; exact-main run `29944461461` PASS | CI is not provider operation, learning validity, accessibility-session, or rollback proof. |
| Local engineering gates | 529 application tests, 13 evaluator-contract tests, 54 offline fixtures, lint, typecheck, and optimized build PASS | Offline fixtures are not live-model evaluation. |
| All-World runtime | 18/18 conformance PASS | The result covers four current authored Worlds and local/non-durable receipts only. |
| Local/public browser | 70 pass / 22 intentional skips / 0 fail; terminal routed conformance 8/8 | Automated browser coverage is not manual AT, Safari, Firefox, or representative-learner proof. |
| Public verifier | final local/public bound verifier 187/187 PASS | The bound verifier proves the exact engineering deployment tuple only. |
| Runtime/HTTP scan | public error-level and 5xx scans empty | Empty scans do not establish alerting, rollback operability, provider quality, or outcomes. |
| Runtime and cloud/provider state | `fallback_only`; request-only BYOK; cloud accounts, evidence sync, and all managed providers disabled | No live provider, cloud account, or durable cloud-evidence operation is claimed. |

The deployment `dpl_4eY4F3vMV1hado7TG1BZN11Mgzvt` (immutable URL prefix `https://forge-learning-9i0915bo1...`) was rejected and replaced because its `build_time` and digests were unknown. Read-only unbound workflow `29945127093` is `DEPLOYMENT_BLOCKED`, not a product/public verifier failure: it failed 184/187 because committed/public lock digest `12f19d...` differed from runner post-install digest `f43d90...` and retained metadata was empty. Lock-digest reproducibility remains an operations follow-up. Rollback target `dpl_pLpQmuCP43Mc3tNt1nErf8MRH2v2` at `98687ea` is retained; rehearsal is `NOT_EVALUATED`.

Live providers, manual assistive technology, Safari, Firefox, representative learners, efficacy/retention, durable cloud evidence, broad curriculum, homeschool readiness, and certification remain `NOT_RUN` or unproven. The release state remains `DEPLOYED_CANDIDATE`, never `PRODUCTION_VERIFIED`.

### Wave 3 amendment for `98687ea`

Wave 3 adds shared-runtime hardening, retained runtime-binding validation, explicit return-proof scheduling policy, and the Ratio runtime migration. Primary Source Reasoning and Proportional Reasoning are now the two shared-runtime Worlds; Force and Motion and AI & Learning remain unmigrated. Ratio requires a distinct post-compiler test prediction, domain-owned relationship evidence, proof isolation, canonical new capability IDs, and one bounded local receipt. It still has legacy/incomplete source provenance, no delayed scheduler, no projector route binding, and no durable authority.

| Gate | Recorded result | Boundary retained |
| --- | --- | --- |
| CI | Run `29932913141` PASS on exact SHA | CI is not educational validity, rollback, provider, or database operation. |
| Unit/evaluator | 465 application + 13 evaluator-contract tests PASS | Live model/provider evaluation was NOT RUN. |
| Local verifier/browser | 182/182 verifier PASS; 61 browser pass / 21 intentional skips / 0 fail | Manual assistive-technology sessions and learning research remain unrun. |
| Ratio deep access | Keyboard journey, focus/announcement, 320px compiler/table/proof/evidence, reduced motion, forced colors, proof isolation, and reset PASS | Manual screen reader and multi-mounted-instance automation remain unrun. |
| Public verifier | Final bound 182/182 PASS as `DEPLOYED_CANDIDATE` | Live evaluation and rollback rehearsal remain `NOT_EVALUATED`; terminal status is not claimed. |
| Public browser | Passing exit: 60 pass, 21 intentional skips, one mobile request timeout passed retry; focused repeats produced two direct passes and one first-attempt timeout passed retry | The operational flake is retained; it is not rewritten as a clean unique-test pass. |
| Runtime/HTTP scan | Deployment error log and 5xx scans empty | Empty scans do not prove drains, alerting, or rollback operability. |

The older Wave 2 record below remains historical evidence for `6e95a33`; it is not the current public tuple.

The earlier principal matrix below remains historical evidence for its named SHA only. It is not silently inherited by a later release. The accepted Wave 2 chain is `4dab5d9` + `f4179e9` for the ADR-001 projector/coherence repair, `48ea3fd` + `30e1dde` + `2cf81ab` for the availability-only pathway surface, `b5b3170` for release-verifier coverage of `/pathways`, and released source `6e95a33`. The release proves an exact public engineering tuple, not durable/authenticated authority, SQL persistence, application runtime binding, live provider/model evaluation, broad curriculum, recommendation, homeschool readiness, certification, child cloud auth, or learning validity.

### Post-release evidence for `6e95a33`

| Gate | Recorded result | Boundary retained |
| --- | --- | --- |
| CI | Run `29927061567` PASS | CI health is not educational validity or provider/database operation. |
| Unit/evaluator tests | 431 application tests + 13 evaluator tests PASS | No live model/provider call was run. |
| Local and public verifier | 182/182 PASS | Verifier success identifies the deployment tuple; it does not create durable evidence authority. |
| Local and public optimized browser | 61 pass / 21 intentional skips / 0 fail | This is a rendered engineering gate, not assistive-technology-session, efficacy, or broad access proof. |
| Runtime and HTTP scan | Runtime errors empty; exact-deployment 5xx scan empty | Empty scans do not prove rollback operability, provider quality, or learning outcomes. |
| Live model | NOT RUN | No live model/provider quality, privacy, cost, or availability claim. |

### Promotion and rollback incident record

The staged deployment `dpl_BLUz...` had exact health identity. The promoted clone `dpl_37...` lost build-time/digest fields and failed release identity. On Hobby, rollback to the prior known-good deployment was refused because only one-step history was available. Direct production deployment `dpl_CBzgm4fAVqKy2U8MVLidiTAWTTJD` replaced the failed clone and supplied the exact released tuple above.

Rollback deployments `dpl_DcKE...` at source `cd418b8` and `dpl_HPts...` at source `79053` remain READY. The rollback rehearsal is `NOT_EVALUATED`: the CLI target attempt failed. READY rollback artifacts are not rollback-operability evidence.

The principal ran these commands on clean `main` at `2e6dc1e` before the governance correction that adds this matrix:

| Command | Result | What it proves | What it does not prove |
| --- | --- | --- | --- |
| `pnpm lint` | PASS, zero warnings | Current TypeScript/application files satisfy the configured lint gate. | Runtime behavior, learning validity, security, or access. |
| `pnpm typecheck` | PASS | Current compiled TypeScript contracts are internally type-consistent. | Schema compatibility with live providers or databases. |
| `pnpm test` | PASS: 23 files, 182 application tests; 1 file, 9 evaluator-contract tests | The inspected unit/component/contract fixtures execute successfully. | Live provider, live database, production, human, retention, or efficacy behavior. |
| `pnpm eval` | PASS: 54 fixture inputs; authored probe invariant passes; clear-fixture baseline 29/38 | The offline interpretation fixture runner is valid and reports its bounded baseline honestly. | Live model quality; the live suite was not run because no key was present. |
| `pnpm build` | PASS: optimized Next.js build; 14 application/API routes listed | Current application compiles in production mode. | That the same source is deployed or production-operable. |
| `PLAYWRIGHT_BASE_URL=http://127.0.0.1:3117 pnpm test:e2e` | NOT GREEN | A unique-port browser run loaded the application and exposed a concrete test-contract failure. | A complete browser matrix. The run must be repeated after correction. |

The browser failure is in `tests/e2e/forge-expansion.spec.ts:45`: a global `getByRole("img")` count expects one image, but the valid accessible Exit-world icon creates a second image role. The page snapshot shows the intended historical source image loaded. The test must select the named/figure-scoped source image; the product icon must not be hidden to satisfy a brittle count.

Every integrated candidate must rerun the full matrix on its final exact SHA and retain machine-readable/browser artifacts. The results above cannot be inherited silently by future commits.

## 3. Objective requirement matrix

| ID | Full-objective requirement | Current authoritative evidence | Disposition | Exact missing or conflicting proof | Owner / prerequisite |
| --- | --- | --- | --- | --- | --- |
| GOV-01 | One current authority for the broad FORGE mandate | `AGENTS.md`; `FORGE_PRODUCT_SPEC.md`; `docs/program/**` | `PROVEN` by this governance change | The former top-level rule named the one-World ModelShift spec as global authority. `AGENTS.md` now makes it historical and names the FORGE authority order. Future workers must use the updated base. | Principal; propagate to every lane |
| GOV-02 | Honest claim boundary | `README.md`; `docs/FORGE_DELIVERY_GATES.md`; program constitution | `PROVEN` for current copy | Current copy calls the product a C1/DG1 candidate and rejects homeschool, efficacy, safety, and education-replacement claims. This proves copy discipline, not readiness. | Principal maintains |
| UX-01 | A universal question-first learner entrance | `ForgeHome.tsx`; planner schema/routes/tests; contestable exploratory-map preview at `a0eb075` | `PARTIAL` | Known questions route safely. Unknown questions now preserve the learner's wording and expose four review gates plus a local unsaved revision note, but the preview is not connected to a reviewed map, resource, project, learner account, or complete journey/path memory. | Kernel, Pathways, Experience |
| MAP-01 | An intent becomes an editable reviewed capability map rather than a generated course | Proposed ADR-009; fixture compiler `e7f5110`; exploratory preview `a0eb075` | `PARTIAL`; fixture compiler and contestable local preview only | The pure compiler validates `LearningIntent` and capability-map packages against caller-asserted unverified inputs, and the preview makes missing source authority visible. There is no trusted curriculum/source/reviewer identity, persisted learner edit, compiler-to-UI binding, runtime route, server entitlement, publication, or adult journey. | PG4; trusted source/reviewer authority, then W6-F |
| RESOURCE-01 | External text/video/resources are reviewed, replaceable, accessible, and lifecycle-governed | Fixture resource catalog and disabled YouTube adapter at `50e55fe` | `PARTIAL`; local candidate/eligibility contracts only | Content-addressed policy/review/lifecycle checks and fail-closed metadata eligibility exist. There is no network discovery, credential, persistent observation/reviewer authority, live refresh/delete/drift operation, click-to-load player, transcript/media handling, ranking, assignment, or external-provider failover. | Source authority, persistence, W6-H, and separate connector enablement |
| VISUAL-01 | Visuals and simulations are semantically authoritative and access-equivalent | Existing bounded World representations; fixture representation registry at `9a17a32` | `PARTIAL`; local registry and current-World alternatives only | Exact representation/dependency/review closure and bounded authored deterministic frame alternatives can be validated in fixtures. There is no published cross-domain registry, generated-visual review authority, runtime integration, broad representation population, or representative nonvisual equivalence proof. | W6-D runtime integration plus accessibility/domain review |
| PROJECT-01 | Every substantial route leads to safe practical work and individual defence | Fixture project/practice compiler at `488e721` plus `1c3a4c4` | `PARTIAL`; fixture compilation only | Exact map/target/prerequisite/project/representation/practice/proof/provenance/return bindings fail closed, but completion remains self-declared and unverified. There is no learner identity, durable artifact store, live safety/material review, project UI/runtime, individual defence, external validator, or project-to-trusted-proof path. | PG6; PG1/PG5 prerequisites and runtime/review authority |
| EDUCATOR-01 | Teacher/TA mode improves decisions and net workload without surveillance | Proposed strategy and W6-G | `REQUEST_ONLY`; operation/evidence `MISSING` | No named educator approval port, learner-visible review, exception queue, workload baseline, professional learning, or teacher study exists. | W6-G; identity/rights plus educator research |
| FUTURE-01 | AI-era roadmap responds to evidence rather than model-launch hype | Scenario thesis and PG9 plan | `REQUEST_ONLY` | The first scenario/signpost packet exists, but no repeated quarterly observatory, owner, decision log, provider-switch proof, or public-interest access result exists. | PG9 after PG3/PG4/PG7 |
| AUDIT-01 | An independent whole-system principal audit reviews the frozen real-world architecture and implementation | `PRINCIPAL_PRODUCTION_AUDIT_MANDATE.md`; SOL Ultra task `019f8e78-550b-7150-82d5-2acb958dab37`; final recommendation-only disposition | `PROVEN` for one bounded read-only audit; production verdict `NO-GO` | The audit identified frontend, release, auth/tenant/consent/quota, source/reviewer, learning-validity, minor/homeschool, manual-AT, supply-chain, and economics gates. Frontend P1s are repaired; production verification remains blocked by later exact deployment evidence. The audit cannot promote implementation, educational validity, or release state. | Close findings through separately accepted lanes; rerun only on a later frozen candidate |
| WORLD-01 | Multiple rigorous subjects, not physics only | Four `/learn/*` routes; retained internal Argument & Evidence package; domain reducers/validators | `PROVEN` for four public Worlds plus one retained internal package; `PARTIAL` for the objective | Mechanics, proportional reasoning, AI/source evaluation, and primary-source reasoning are real bounded public Worlds. Argument & Evidence is executable internal code but has no public route, trusted source binding, curriculum entry, or public browser/manual-AT proof. Five bounded packages are not a broad foundation, complete subject map, or learn-anything system. | Kernel plus domain/content owners |
| WORLD-02 | Deep self-study across broad foundations and learner-chosen depth | Roadmap catalog and PG4 plan | `MISSING` | No reviewed entitlement map, prerequisite/capability graph, W0 foundation library, deep sequences, multilingual/access variants, or broad arts/health/civic/practical coverage. | Pathways PG4 after Kernel PG2 |
| RUNTIME-01 | One modular World authoring/runtime contract | `src/forge/contracts.ts`; public/internal runtime split; four public adapters plus retained W5-E internal adapter; receipt-only compatibility projector | `PARTIAL`; four routed Worlds and one retained internal package accepted | All four public Worlds have runtime bindings and passed 18/18 public conformance plus terminal routed browser 8/8. W5-E uses the same contract internally and passed focused domain/runtime/component review, but has no public route, public receipt authority, or routed browser/manual-AT proof. All receipts/projector output remain local, non-durable, unauthenticated, and not trusted pathway evidence. | Authenticated application binding; separate W5-E source/curriculum/publication/access gates |
| RUNTIME-02 | Domain truth remains domain-specific | Five World reducers/validators; public/internal runtime projection; canonical validator registries | `PROVEN` for four public slices plus one retained internal slice | Each domain owns reducer/validator truth while the shared runtime owns sequencing/policy. W5-E derives choices, results, and answers from its sole authored JSON fixture. Public all-World reducer equivalence and W5-E bounded pass/fail/malformed behavior passed; construct validity beyond the authored tasks remains untested. | Research validity and future World review |
| PROOF-01 | Help visibly leaves before unfamiliar proof | Four public World reducers/components; retained internal W5-E component; shared runtime proof locks; terminal routed browser 8/8 | `PARTIAL`; four routed flows plus one internal component contract | All four public runtime Worlds reject instructional/model/replay actions in proof while retaining typed access; the terminal matrix rendered bounded results and one receipt-derived local entry per route. W5-E enforces withdrawal and single-shot transfer in component/runtime tests but has no routed browser/manual-AT proof. Complete nonvisual equivalence and manual AT proof remain absent. | Experience/manual AT audit |
| PROOF-02 | Independent evidence is credible and replayable | Device ledger; proof record helpers; additive v2 in-memory journal/projector | `MISSING` for independent/durable authority | ADR-001 can seal/replay typed local events with frozen caller-supplied task/content/validator facts, but it has no server proof nonce, route binding, authenticated append, durable storage, or visible conflict reconciliation. Current result calculation and storage remain client-controlled. | Trust PG1 + Kernel PG2 |
| EVID-01 | Learner-owned bounded evidence, export, and deletion | Device ledger plus four bounded public runtime receipts and terminal browser tests; retained W5-E internal receipt | `PROVEN` for privacy-minimal routed device records; `PARTIAL` overall | All four public receipts name task/content/validator/support/access/source status and exclude raw learner prose, but remain client-controlled, non-durable, and source-incomplete. W5-E's internal receipt also excludes raw prose but is deliberately not public-ledger authority. Device records remain mutable local storage; no durable authenticated application authority exists. | Trust projector/persistence after additive review |
| EVID-02 | Every World emits one canonical evidence vocabulary | Four bounded public receipts compile through W5-B; W5-E has an exact internal-only seven-event compiler at local baseline `7ba70ef` | `PARTIAL`; exact compilers accepted, not application or durable authority | The public compiler re-runs each canonical validator and pins receipt, retained manifest, fresh package/runtime digests, exact task code, support/access/source facts, package-owned limitations, event identities, and coherent in-memory replay. W5-E compiles internally but is deliberately unattested and rejected by the public ledger. Neither path emits raw learner prose. No route invokes durable append, no authenticated/server proof nonce exists, and v1 device writers remain. The additive SQL contract is accepted locally but is not application-bound. | Bind only after separately accepted application/auth and configured-project gates |
| RETURN-01 | Delayed return proof uses a fresh reviewed task | World manifest limitations; ledger date scheduling | `MISSING` | A local date can be stored, but no reviewed delayed task family, scheduler, reminder policy, returned proof session, or delayed evidence ingestion exists. | PG1/PG2 then research validity |
| SOURCE-01 | Published factual content is source-grounded and reviewable | Accepted pure source package/policy/replay contracts at local main `07f5f28` within `de84fcb`; current World legacy tuples | `PARTIAL`; deterministic candidate boundary accepted | Content-addressed source/policy identity, Unicode/exact locator checks, scoped review completeness, temporal replay, correction/withdrawal/expiry invalidation, candidate bindings, and explicit non-authority fields now exist locally. Current World metadata remains legacy-incomplete; there is no acquisition service, external authenticity, rights clearance, durable accountable-human review, or publication operation. | Curriculum graph, then separately authorized durable review/publication service |
| SOURCE-02 | Unknown questions cannot fabricate reviewed curriculum | Deterministic planner and invented-ID/source rejection tests; contestable preview `a0eb075`; fixture resource contracts `50e55fe` | `PROVEN` | Unknown topics remain explicitly exploratory and cannot invent World/source IDs. Fixture review/eligibility contracts now exist, but there is no trusted, persistent, operational source-acquisition/review/publication workflow to turn them into curriculum. | Source/reviewer authority plus Packet F publication pipeline |
| AI-01 | Provider-neutral, schema-bounded draft generation | Fixed OpenAI, Anthropic, Gemini, OpenRouter adapters; allowlists; budgets; strict schema; mock/eval tests | `PROVEN` for mocked transports; `PARTIAL` overall | Fixed timeout/token/cost bounds, closed errors, request correlation, and default-off redacted live evaluation exist. No authorized live-provider evidence, durable quota/rate limiter, provider privacy decision, or enabled adult connector exists. | Separately authorized provider operations |
| AI-02 | AI never self-publishes or grades proof | Locked Studio page/API; replay-validated local review record; no publication route | `PROVEN` for the current non-publication boundary | Generated output remains an editable draft; fabricated approval, publication, and grade fields fail. Reviewer identity and records are local/non-durable, so no safe publication authority exists. | Durable identity/review service + principal publication gate |
| AI-03 | Reviewed draft-to-World publication and withdrawal | Local draft/source/factual/pedagogy/access/safety/proof/approved/rejected/withdrawn machine with immutable refs | `PARTIAL`; local Packet F state machine `ACCEPTED` | Deterministic replay and complete source-receipt shape exist, but there is no durable reviewer queue, authenticated decision authority, source authenticity, World compiler/publication event, withdrawal operation, or rollback evidence. | Source/review service + publication architecture |
| AGE-01 | Child modes fail closed across every entry route | Integrated server/client World age gate; device-profile validation; planner gate; forged-query/corrupt-profile/deep-link/reload E2E on `9a4068f` | `PROVEN` for the current routed surface | Both child-capable direct World routes now require a valid local device selection; child mode additionally requires grown-up confirmation. URL audience hints never grant access. This is a fail-closed routing result, not verified age or guardian identity; every future route must pass the same conformance gate. | Packet E all-World conformance for future routes |
| AGE-02 | A checkbox/local profile is never verified age or guardian authority | Structurally disabled cloud-auth authority; account-action and environment-forgery tests on `9a4068f` | `PROVEN` for the current disabled-cloud boundary; cloud operation `MISSING` | Complete-looking Supabase environment values and adult self-attestation still produce no cloud auth authority. Any future enablement requires separately approved CAPTCHA, durable distributed abuse control, authenticated age/guardian policy, and live negative proof. | Trust follow-up only after explicit cloud authorization |
| AUTH-01 | Adult identity and private continuity are production-operable | SSR helpers; structurally disabled authority; staged SQL and fresh/legacy-upgrade fixtures | `PARTIAL` architecture; `MISSING` operation | Local migration/RLS/trigger structure is executable and legacy-safe, but no configured-project signup/refresh/signout, CAPTCHA/limiter, two-account live isolation, explicit per-item sync, export/deletion completion, recovery, or consent-revocation operation exists. | Packet E runtime, then approved disposable project |
| DATA-01 | Durable append-only event/evidence spine is authoritative | Accepted additive v2 contract at local code baseline `7ba70ef`; exact compiler golden; fresh/upgrade fixtures; 12 overlapping v1/v2 races | `PARTIAL`; local persistence contract accepted | Exact schema parity, global UUID/aggregate/idempotency arbitration, transactionally coupled journal/head/outbox, immutable identity/documents, bounded support/access/source facts, truncate refusal, and upgrade-safe new error-code grammar pass locally. Application flows still do not append to it; hosted RLS/auth, backup/restore, recovery, rollback, configured-project isolation, and production durability remain absent. | Bind only after separately accepted application/auth and configured-project gates |
| DATA-02 | Identity, evidence, consent, sharing, and deletion remain separate | Program architecture; staged schemas | `PARTIAL` | Separation is designed but no end-to-end rights operation exists. Identity must not silently turn local evidence into cloud data. | Trust B; PG1 goal / DG2 gate |
| PATH-01 | Homeschool/self-study pathways preserve breadth, choice, access, relationships, protection, and portability | Packet C availability surface plus accepted pure curriculum graph at local main `29e1aae` | `PARTIAL`; untrusted graph contract accepted | The graph deterministically exposes all nine areas as caller-asserted exact matches, review candidates, or explicit gaps; source corrections fail closed and every routable route remains null. It does not authenticate registry/source authority or drive the public pathway map. No schedule, recommendation, learner choice operation, jurisdiction pack, attendance, accreditation, coverage sufficiency, or homeschool readiness exists. | Build and accept the trusted registry/source adapter; later learner-choice and jurisdiction gates remain separate |
| PEOPLE-01 | Projects and helpful human relationships are operational and safeguarded | Home roadmap copy; PG6 architecture; fixture project/practice compiler `488e721` + `1c3a4c4` | `PARTIAL` for a local project contract; people operation `MISSING` | The fixture compiler models exact artifact provenance, critique/revision, proof and return bindings, but no learner/runtime workflow, durable artifact, external validator, verified role, reporting/blocking, contact boundary, expiry, supervision, appeal, or individual defence operates. | PG6 after PG1/PG5; separate minor gate |
| ACCESS-01 | One coherent accessible learner instrument | Shared tokens/primitives; four runtime Worlds; local/public 70 pass / 22 intentional skips / 0 fail; terminal routed 8/8 | `PARTIAL`; bounded automated browser checks | All four Worlds passed the routed desktop/320, keyboard, reduced-motion, forced-colors, proof isolation, result, and receipt checks. Screen-reader sessions, complete semantic/nonvisual equivalence, Safari, Firefox, voice, switch, magnification, and representative learner sessions remain `NOT_RUN`. | Assistive-technology and cross-browser review |
| SAFE-01 | Security/privacy/abuse boundaries support enabled cloud/provider use | CSP nonce; SSR/cookie/origin/body/schema checks; fixed providers; locked Packet B/F cloud/provider boundaries | `PARTIAL`; cloud/provider enablement remains forbidden | Public auth and Studio provider calls fail closed before authority/body/provider access. No CAPTCHA/durable limiter, configured-project/provider proof, provider privacy decision, prompt/source-poisoning operation, deletion/backup drill, incident operator, or enabled-cloud evidence exists. | Separately authorized Trust/provider operations |
| EVAL-01 | Engineering behavior is freshly verified | Exact deployed source `35a1d2c`: 790/790 application and 13/13 evaluator-contract tests; 54/54 offline fixture-validity checks; lint, typecheck, diff checks, optimized build, 38-asset local boundary; full local browser 74 pass / 22 intentional project-scoped skips / 0 fail across 96 cases; public verifier 206 pass / 4 fail; live Chrome desktop-keyboard and 320px smoke with zero console warnings/errors | `PROVEN` for the named bounded application/browser gates; release identity `CONTRADICTED` by the asset receipt | Live model evaluation was not run; the rule baseline is 29/38. No configured database/provider check, Safari/Firefox, manual AT, learning-validity study, reviewed-resource operation, durable evidence, or broader-claim evidence exists. The four public-verifier failures are release blockers, not passing evidence. | Release owner repairs provider receipt; research/access/provider owners retain distinct gates |
| EVAL-02 | Measurement validity, delayed retention, and learning effects are known | Evaluation plans and fixture harness | `MISSING` | No preregistration, equivalent task-family validation, rubric reliability, evaluator disagreement, contamination study, representative learner/access study, delayed retention, strong-baseline comparison, or independent review. | Research PG7 |
| OPS-01 | Current source/test/deployment identity is trustworthy | Exact source `35a1d2c5bd28c7b794b26414c6bdfe8f18097027`; `https://modelshift.vercel.app`; `dpl_Er7rVecXt3iga56P4uPDoLnWt9V4`; immutable URL `https://forge-learning-7a63ywsp5-ranapriyanshs-projects.vercel.app`; Vercel metadata and public health tuple | `CONTRADICTED` for retained public-asset identity; candidate `DEPLOYMENT_BLOCKED` | Source, deployment, project, build time, lock/content/evaluator digests, route health, and disabled-service posture agree. The manifest asset digest `b1f8d95f...` does not equal the platform-emitted `aad493295...`; a same-source no-promote experiment emitted another digest `83e1de1a3...`. A post-build provider receipt and a later passing exact-source verification are required. | Release-provenance repair lane |
| OPS-02 | CI, health, observability, artifact retention, and rollback are operational | CI `29944088585` + `29944461461`; empty public error-level/5xx scans; rollback target `dpl_pLpQmuCP43Mc3tNt1nErf8MRH2v2` (`98687ea`) | `PARTIAL`; rollback operation not evaluated | Rollback rehearsal is `NOT_EVALUATED`. Read-only unbound workflow `29945127093` is `DEPLOYMENT_BLOCKED` at 184/187 for a committed/public `12f19d...` versus post-install `f43d90...` lock-digest mismatch and empty retained metadata; lock-digest reproducibility is an operations follow-up, not a product/public verifier failure. | Principal operations rehearsal + lock-digest/incident follow-up |
| DEPLOY-01 | A verified public deployment runs the integrated FORGE source | `35a1d2c`; `https://modelshift.vercel.app`; `dpl_Er7rVecXt3iga56P4uPDoLnWt9V4`; immutable URL; exact public health SHA; live keyboard/320px smoke | `PROVEN` only for public exact-code operation; release identity `DEPLOYMENT_BLOCKED` | The deployment runs the four bounded shared-runtime Worlds and adult fixture-only source-corroboration presentation, and its learner surface passes the named smoke checks. It does not have a passing retained provider-asset receipt and therefore is not a verified release. It also does not prove durable evidence, cloud auth, provider operation, reviewed resources, assignment/proof/return authority, manual accessibility sessions, curriculum coverage, efficacy, homeschool readiness, certification, or rollback operability. | Repair receipt, deploy a later candidate, rerun the complete verifier |
| CLAIM-01 | Production-grade, broad homeschool/lifelong objective is achieved | Entire matrix | `CONTRADICTED` | Multiple foundational requirements are missing or contradicted. The strongest current statement remains a C1 interactive foundation/DG1 candidate with four bounded Worlds. | Full PG0–PG9 program |

## 4. Goal-gate status

These are the `PG#` dependency goals from `MASTER_PLAN.md`, not the distinct `DG#` delivery/claim gates.

| Goal | Current status | Evidence needed to exit |
| --- | --- | --- |
| PG0 Program control and truth | `PARTIAL`; A-F dispositions and integration ledger complete | One current public release tuple plus future worker/release amendments must preserve the same evidence discipline. |
| PG1 Trust core and durable replay | `PARTIAL`; exact all-World v2 compiler, in-memory replay, and local additive SQL contract accepted | No authenticated/server proof authority, application append, configured adult-only hosted operation, backup/restore, recovery, rollback, or production durability proof. |
| PG2 Learning Kernel and World factory | `PARTIAL`; four-World public runtime, retained internal W5-E package, exact compilers, pure curriculum graph, and local SQL parity accepted | W5-E and every future World still require independent trusted-source, curriculum, content, routed browser/access, publication, and release acceptance before becoming public. No current receipt is durable/authenticated authority. |
| PG3 AI lesson intelligence/publication | `PARTIAL`; structurally locked Packet F slice accepted | Durable authenticated review/source services, authorized quota/abuse/privacy controls, reviewed compiler/publication/withdrawal events, and separately approved live-provider evidence. |
| PG4 Foundations and learner frontier | `PARTIAL`; availability-only public map, pure nine-area graph, fixture capability-map compiler, and contestable unknown-question preview accepted | A trusted registry/source/reviewer adapter, durable reviewed receipts, compiler-to-UI/runtime binding, learner choice persistence, and actual broad/deep paths remain absent. |
| PG5 Unified learner experience/access | `PARTIAL`; Packet A, typed access on four runtime Worlds, and all-World automated browser conformance accepted | Screen-reader sessions, complete semantic alternatives, all-state nonvisual equivalence, real assistive-technology review, and Safari/Firefox remain unrun. |
| PG6 Projects, people, contribution | `PARTIAL`; fixture project/practice compiler only | PG1/PG5 prerequisites, learner/runtime binding, durable project provenance, independent defence, verified roles, consent, reporting, expiry, appeals, safeguarding, and real operation. |
| PG7 Measurement validity/research | `MISSING` | Construct/task-family validity, reliability, contamination, transfer/retention, representative access, preregistered strong-baseline studies. |
| PG8 Homeschool/institution operations | `MISSING` | All prerequisites plus named jurisdiction, records/rights, accommodations, complaints, workload, continuity, legal/safeguarding/access review, portability acceptance. |
| PG9 Capability commons/future observatory | `REQUEST_ONLY` | Repeated sourced signpost reviews, provider/resource portability, open/low-resource parity, named decisions, and evidence that roadmap updates preserve claims and learner rights. |

## 5. Accepted principal decisions required by cross-lane integration

The normative resolutions for D-01 through D-06 are in `ARCHITECTURE_DECISIONS.md`. The summaries below remain an audit index; they are no longer open choices for workers.

### D-01 — Canonical evidence outcome and assistance mapping

Unify, without losing provenance:

- device ledger: `proved | not_proved`;
- shared contract: `demonstrated | partial`;
- event projection: `proved | not_proved | open_question`;
- assistance labels including `ai`, `model`, and `model_interpretation`.

The canonical journal vocabulary must represent uncertainty, contamination, accommodation, cognitive assistance, and corrections. UI/device/cloud projections may map from it; they may not invent stronger outcomes.

### D-02 — Content and World lifecycle mapping

Reconcile:

- pack lifecycle `draft | released | suspended`;
- SQL lifecycle `draft | review | published | disabled | retired`;
- event lifecycle `published | disabled | superseded`;
- lesson review lifecycle from draft through reviewed/withdrawn.

Human approval does not itself publish a World. Publication is a separate reviewed event with exact package/source/policy versions and rollback target.

### D-03 — Canonical source identity

World manifests, source packages/items/claims, Studio source needs, rights records, review decisions, and event replay must share stable source/version/checksum/reviewer identifiers. A URL plus `reviewed: true` is not sufficient publication provenance.

### D-04 — World runtime extension rule

Packet E must extend existing `LearningWorldPack`/registry identities rather than create parallel package, proof, or evidence authorities. Domain validators remain authoritative for subject truth; runtime owns only common protocol and authorized effects.

### D-05 — Age and guardian enforcement

Device preferences remain UX inputs. Every route/API/action independently enforces the permitted age mode. Under-18 cloud identity and all sharing/contact remain disabled until server-owned relationship, consent/assent, recovery, jurisdiction, and safeguarding gates pass.

### D-06 — Release identity tuple

Every candidate records exactly:

```text
source SHA
tested SHA and retained artifacts
immutable deployment ID/URL
public alias and alias-resolution time
build/runtime mode
enabled cloud/provider flags without secrets
database migration identity
browser/CSP/error verification
rollback target and rehearsal evidence
```

No field may be inferred from a branch name, dashboard label, or previous deployment.

## 6. Ordered stop-ships

1. Close `AGE-01` and `AGE-02` before enabling any cloud identity or presenting child routes as guarded.
2. Do not call browser-local outcomes independently verified evidence until `PROOF-02`, `EVID-02`, and `DATA-01` pass.
3. Keep accepted D-01 through D-05 invariants enforced before any persistence, runtime, or lesson-publication follow-up.
4. Keep managed provider keys and live paid generation disabled until Packet F plus authenticated budgets/abuse controls pass.
5. Do not publish generated curriculum until durable source, rights, factual, pedagogy, access, safety, and proof reviews are linked to an immutable package.
6. Keep the complete browser matrix green and add the still-missing screen-reader and all-state nonvisual-equivalence evidence.
7. Even with Packet D integrated, call no public URL the current FORGE release until the exact final tuple passes the principal gate.
8. Do not claim homeschool readiness before actual PG1–PG8 prerequisites, a named jurisdiction/learner band/context, and independent legal/safeguarding/access decisions.
9. Do not claim learning, retention, efficacy, or broad transfer before PG7 measurement-valid and comparative evidence exists.

## 7. Current worker acceptance focus

| Packet | Principal will reject the handoff if… |
| --- | --- |
| A — Experience | It hides valid semantics to satisfy a brittle test; lacks all-route state evidence; or calls 390 px proof “320 px.” |
| B — Trust/auth | It trusts the adult checkbox, creates a fourth evidence vocabulary, auto-syncs local evidence, exposes service role, or promotes mock/RLS text to live proof. |
| C — Pathways | It produces schedules/compliance claims instead of a pure deterministic review contract, or turns missing evidence into entitlement completion. |
| D — Release | It cannot identify the blocked deployment cause and exact current tuple, introduces unapproved deploy capability, or reports engineering tests as educational validity. |
| E — Kernel | It creates parallel World/package/evidence truth, imports subject answers into the kernel, or migrates UI without proof/access/evidence equivalence. |
| F — Lesson Intelligence | It accepts arbitrary model/endpoint authority, self-approves, logs learner text/keys, spends live credits, grades proof, or treats source needs as verified sources. |

## 8. Completion rule

The durable objective is complete only when every requirement in Section 3 is `PROVEN` at the exact required scope, every PG0–PG9 program goal has its signed evidence packet, every applicable DG0–DG10 delivery gate has passed, no stop-ship remains, and one frozen integrated SHA passes local, provider/database where authorized, rendered-access, production, rollback, and human-validity gates. Until then, FORGE remains an actively built learning-system architecture with bounded working Worlds—not a production-grade homeschool or education replacement.
