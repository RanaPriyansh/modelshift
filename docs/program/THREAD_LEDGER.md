# FORGE Worker Thread Ledger

## Operating rule

Workers implement bounded slices in isolated worktrees. They do not edit the principal-owned files in `docs/program/`, merge to `main`, push shared branches, deploy, enable provider/cloud credentials, or mutate a live database. Each lane reuses its thread for subsequent goals.

**Wave 4 release boundary:** released source is `c7d401101791faa80ac0ba61c73044928a2da7b6`, public alias is `https://modelshift.vercel.app`, accepted production deployment is `dpl_6MUabxtnuMVzwxHf1vbM5Y1LxNfs`, and immutable URL is `https://forge-learning-2sh2loxy1-ranapriyanshs-projects.vercel.app`. Exact-main CI `29944461461`, pre-main CI `29944088585`, local/public browser 70 pass / 22 intentional skips / 0 fail, all-World conformance 18/18, terminal routed conformance 8/8, and final local/public bound verifier 187/187 passed. The bound verifier status is `DEPLOYED_CANDIDATE`; live evaluation and rollback rehearsal remain `NOT_EVALUATED`. This later documentation-only amendment is not deployed. No lane may describe the release as durable authority, live provider operation, cloud auth, broad curriculum, certification, or homeschool readiness.

**Wave 5 current boundary:** [`WAVE_5_PLAN.md`](./WAVE_5_PLAN.md) remains the active architecture packet. Principal accepted local code baseline `7ba70ef60c038883bd4ae9ca89c974d9bec61cea` includes independently accepted W5-A immutable lock identity, W5-B all-World receipt compilation, both pure W5-D contract slices, the local-only W5-C additive SQL contract, the nine-area caller-asserted curriculum graph, and W5-E as a retained unavailable internal package; this later ledger amendment is documentation-only. On that exact code SHA, 626 application tests, 13 evaluator-contract tests, 54 offline evaluation fixtures, typecheck, lint, diff checks, and the production build passed. The production build emitted 36 static assets, no source maps, and no W5-E marker; integrated browser verification passed 70 with 22 intentional skips and zero failures. `/` returned `200`, while the W5-E learner route, authored fixture, provenance path, and legacy ModelShift route each returned `404`. The exact local PostgreSQL harness also passed fresh and historical-upgrade fixtures plus 12 overlapping cross-version races, deleted `forge_w5c_fresh_1784757613_21516` and `forge_w5c_upgrade_1784757613_21516`, and left zero `forge_w5c_*` databases. It is not deployed. The graph exposes only `caller-asserted-unverified` exact matches with null routable routes; W5-E has no public route or curriculum binding; and the SQL result establishes neither hosted nor production durability. No worker may enable cloud/provider features, migrate a live database, publish a World, push shared main, deploy, or edit principal program documents.

**Wave 6 current boundary:** deployed exact source `04eab4263658725d7a228c67682c40fc469757b1` contains the fixture/pure-module capability-map, resource/YouTube, representation, project/practice, `/paths/source-corroboration` presentation, Wave 6 authority preflight, frontend repairs, deterministic controller, fail-closed `/pilot` review shell, and provider-authenticated post-build receipt. Combined local verification passed 819 application tests, 13 evaluator-contract tests, 54 offline fixture-validity checks, lint, typecheck, optimized 42-asset build with the pilot marker gate, production dependency audit, and the full browser matrix at 76 pass / 28 intentional project-scoped skips / 0 fail. Focused production pilot checks added 4/4 default-denial/storage passes and 3/3 enabled review-fixture passes. The public alias now resolves to READY deployment `dpl_ET6nUWvjeVMEdacWJgCbxVsCT1qn`; public production Chromium passed 5/5 plus 1/1 default pilot denial, and exact-deployment error/fatal logs were empty at inspection. Release verification remains `DEPLOYMENT_BLOCKED` at 207 pass / 8 fail because provider metadata reports CLI source with no `gitSource`/`gitRepo`. Local main after deployment hardens the collector for the observed nested event envelope through `3d1c58c`, with 830 application tests, 13 evaluator contracts, 147 focused release checks across receipt/verifier/health/identity, and independent red-team GO. The pilot shell is review infrastructure, not provider, persistence, entitlement, assignment, cohort, or evidence authority, and W6-006 remains incomplete. No reviewed-resource operation, real assignment, durable proof, minor operation, or homeschool authority follows.

**University-first current boundary:** exact local source `612ab13daaca4122cee48b1204ae9b1f7d0a1357` and tree `893489aa1bc6386d5fb900afb780010b0085f4a5` contain the accepted bounded-input repairs, selected-Chrome Semester Desk focus repair, CI browser evidence receipts, and bounded temporary cleanup. The integrated candidate passed 1,715 application tests, 13 evaluator tests, 54 offline fixtures, typecheck, lint, YAML and Python checks, the helper-source digest check, and a 67-page production build. The build recorded 1,472 files, 71 public assets, artifact digest `sha256:ab8f6e8202d1eed79b363d9b9ff11d21d159efa27181220f62c561968ec95477`, and public-asset digest `sha256:ebd5ea3796ea899f1db87856b19fe8f5da6a6d51bd047f30f3a50e02ab3cf416`. Two independent Luna Max reviews accepted exact receipt source `08b2c661c59515ea0b7e55196b85a49c3769164f`, integrated as `a7edbcf`. A later exact-source review accepted cleanup source `be6ad61b208caf630b23421f946f1642c1013c71`, integrated as `612ab13`. Required GitHub browser execution, manual assistive-technology evidence, target disposition, provider receipt, push, and deployment remain unrun or unauthorized.

## Active lane map

| Lane | Task | Default model | Dispatch status | Existing handoff | Next goal |
| --- | --- | --- | --- | --- | --- |
| Experience and design system | `019f8705-865e-7631-a921-ee4b5690b778` | `gpt-5.6-terra` xhigh | `IDLE`; Packet A `ACCEPTED_ON_MAIN` | Worker chain `e84a395` + `eb19eb4`; integrated as `60b947e` + `f7a3bc4` | Hold for cross-lane regression; resume for screen-reader/nonvisual-equivalence work only after Packet E defines the shared runtime |
| Trust, auth, and private evidence | `019f8705-2825-7cb2-94e1-b30d5aab1447` | `gpt-5.6-terra` xhigh | `IDLE`; Packet B `ACCEPTED_ON_MAIN` | Source chain `9a85524` through `2564234`; current-base integration `30bf5bc` through `9a4068f` | Hold for Packet E canonical persistence runtime and separately authorized abuse-controlled cloud-operation proof |
| Homeschool pathways and entitlement | `019f8704-dc1c-7381-b71b-c0303987251a` | `gpt-5.6-terra` xhigh | `IDLE`; Packet C contract and Wave 2 availability-only map `ACCEPTED_ON_MAIN` | Packet C integrated as `033bb9a`; availability chain `48ea3fd` → `30e1dde` → `2cf81ab` | Hold for durable trusted receipt/source service; no scheduler, recommendation, certification, compliance, curriculum-entitlement, or homeschool-readiness claims |
| Release, evaluation, observability | `019f8f92-8e02-7cf3-a417-a0314852eaa6` | lower-cost implementation lane | `ACCEPTED_ON_MAIN`; live provider event envelope hardened after public observation | Deployed chain `a3c2090` → `5783c6e` → `ed03187` → `16e048e`; local post-deploy chain `5c3941f` → `05f96fe` → `3d1c58c`; 147/147 current focused release checks and independent red-team GO | Authorize Vercel GitHub App access, create a fresh Git-source production deployment, then collect the authenticated receipt; repeating a CLI upload cannot close provenance |
| Adult pilot controller and route shell | `019f8f95-657f-7c53-8e09-aba52437238d` | lower-cost implementation lane | `ACCEPTED_ON_MAIN`; kernel and fail-closed route shell integrated | kernel `07fd770`; shell `694b3fd` → `59cc957` → `ccb0153`; independent red-team GO | Hold as fixture-only review infrastructure until adult server entitlement, reviewed source/resource authority, and real cohort operations exist |
| Learning Kernel and World Factory | `019f8973-ef2f-7071-89a3-d5b81781957d` | `gpt-5.6-terra` xhigh | `IDLE`; four-World runtime and receipt-only projection `ACCEPTED_ON_MAIN` | Wave 4 integration through `c7d4011`; conformance 18/18 and terminal routed 8/8 | Design durable/authenticated authority only after additive evidence/persistence review; local receipts/projector output are neither durable nor trusted pathway evidence |
| AI Lesson Intelligence | `019f8973-f2d9-7710-ba01-451d892f767a` | `gpt-5.6-terra` xhigh | `IDLE`; Packet F `ACCEPTED_ON_MAIN` | Replay `b07d577` → `fa634ea`; integrated as `1e95a15` → `15a7531` | Add a durable human-review queue/source service only after identity, quota, abuse, privacy, and publication authority are separately approved |
| Independent real-world principal audit | `019f8e78-550b-7150-82d5-2acb958dab37` | `gpt-5.6-sol` ultra | `COMPLETE`; production verdict `NO-GO`, bounded candidate operation only | Recommendation-only audit against the prior frozen packet; frontend P1s repaired; release proof remains blocked by new production evidence | Re-run only after a later exact-source candidate closes receipt, authority, accessibility, learning-validity, privacy/safety, and operations gates |
| Course-source Proxy boundary | `019fbc37-64fa-73b1-be31-ff56e6ea6137` | `gpt-5.6-luna` max | `COMPLETE`; principal integrated and repaired client compatibility | Source `b95c54451704b170bf1427c4216bd36dbeee5c38`; integrated as `b15a6c5`, with client-safe correction `49dba0d`; worktree `worktrees/luna-proxy-reflection-20260801` | Hold; no provider, persistence, push, or deployment |
| Structural issue cap | `019fbc37-850a-7f61-8a16-f879dc4fcdee` | `gpt-5.6-luna` max | `COMPLETE`; principal integrated | Source `27cc2bdf859d2eb1719bdecb86a58a70fc9666e0`; integrated as `8ac15d8`; worktree `worktrees/luna-issue-cap-20260801` | Hold; root owns further boundary changes |
| University string and byte limits | `019fbc37-acd9-7a13-9b6a-06570eb61cc3` | `gpt-5.6-luna` max | `COMPLETE`; principal integrated | Source `3e39114eacbae88e73639d8fff079e970b51baae`; integrated as `5b4f1c9`; worktree `worktrees/luna-string-bound-20260801` | Hold; no real learner data |
| Container pre-enumeration limits | `019fbc37-d432-73e2-997a-03015658fe1f` | `gpt-5.6-luna` max | `COMPLETE`; principal integrated | Source `0d024e0274798fbeb6b0c3e227aa4cdf014f0fe9`; integrated as `06b4a8d`; worktree `worktrees/luna-container-bound-20260801` | Hold; root owns further boundary changes |
| Combined hardening review | `019fbc4a-5c6a-7200-9c47-5e69d746e3a3` | `gpt-5.6-luna` max | `COMPLETE`; two findings repaired | Client import defect fixed in `49dba0d`; Protected Study array defect fixed in `a68d20c`; final review found no confirmed defect | Hold; residual browser and assistive-technology proof stays principal-owned |
| Implementation evidence audit | `019fbc4a-8529-7961-ad7b-c39d9e7a31b8` | `gpt-5.6-luna` max | `COMPLETE`; stale claims corrected | Disproved the old Source Review browser claim and proved the aggregate artifact digest is path-bound | Hold; do not promote local evidence |
| Selected-Chrome Source Review check | `019fbc4b-becf-72d1-9fea-8a5a5c1b31ae` | `gpt-5.6-luna` max | `COMPLETE`; initial failure repaired and follow-up passed | `0 of 3` ready state, decision, announcement, pressed state, reset, no application error | Hold; no screen-reader or direct Playwright claim |
| Phase -1 authority audit | `019fbc69-de46-70b1-8d1b-2cf14cf5cdd8` | `gpt-5.6-luna` max | `COMPLETE`; no edits | Confirmed three target choices and participant-operation block at `4283909`; legacy rehearsal authority remains internally inconsistent | Principal must record one target disposition; do not infer it |
| Synthetic tabletop execution | `019fbc6a-1520-76e3-b909-d0ba1dc1e09f` | `gpt-5.6-luna` max | `BLOCKED_NO_EDIT`; no rehearsal or commit | The packet says rehearsal is allowed, but its normative sequence requires target disposition first | Do not execute or describe a completed rehearsal before disposition |
| Seven-drill tabletop audit | `019fbc6a-3ca3-7a53-82f1-892af9759a1d` | `gpt-5.6-luna` max | `COMPLETE`; twelve confirmed gaps | Target, deletion, role, stop, missingness, response, typed-record, and self-binding gaps recorded; denominator arithmetic passed | Repair or replace only after the principal selects the target |
| University browser evidence audit | `019fbc6a-5fca-7c31-84b9-1bd85147b0b3` | `gpt-5.6-luna` max | `COMPLETE`; source gap defined | Existing commands lacked exact retained CI receipts; audit proposed a target-independent receipt | Actual CI browser execution and assistive-technology evidence remain open |
| Semester Desk focus repair | `019fbc70-fa59-7593-9eb2-6fd769863e7d` | `gpt-5.6-luna` max | `COMPLETE`; integrated and selected-Chrome verified | Source `0a833337faba0fc54560064f398cec5d4321c4d3`; integrated as `5e0856d` plus `fb6f3c`; 8 focused tests | Hold; no direct Playwright or screen-reader claim |
| CI browser evidence receipt | `019fbc7a-4d3d-74c3-aa88-39b382f79bba` | `gpt-5.6-luna` max | `COMPLETE`; accepted and integrated | Source `08b2c661c59515ea0b7e55196b85a49c3769164f`; integrated as `a7edbcf`; 102 focused candidate checks and full non-browser gates passed | Run GitHub browser targets; retain receipts and failure artifacts |
| CI receipt adversarial review | `019fbc8c-84ea-7d70-a82a-5e3a2795bff4` | `gpt-5.6-luna` max | `COMPLETE`; `ACCEPT` | 110 focused checks, real list-mode shape probes, replacement probes, current-report re-hash, and ancestor-race probes passed | Cleanup iteration was later bounded; stat-then-unlink remains non-blocking |
| Stable descriptor writer advisory | `019fbc9e-3c7f-7563-8716-43ef4a19955f` | `gpt-5.6-luna` max | `COMPLETE`; advisory only | Defined the inherited-directory-descriptor, temporary-file, hard-link, fsync, process, and post-write checks used by the final repair | No independent candidate from this task was integrated |
| Trusted report hardening review | `019fbd14-4e8e-7250-8771-592ed1519dbb` | `gpt-5.6-luna` max | `COMPLETE`; `ACCEPT` | Exact `08b2c66` and its required parent, schema 2.2, six ancestor probes, eight writer probes, six validator probes, and two timeout probes passed | Actual browser and GitHub-hosted workflow execution remain separate gates |
| Bounded receipt cleanup | `019fbde9-ff21-7e61-ab48-f2ba0fddcbf1` | `gpt-5.6-luna` max | `COMPLETE`; accepted and integrated | Source `be6ad61b208caf630b23421f946f1642c1013c71`; integrated as `612ab13`; 43 focused tests and independent 512, 513, and concurrent-addition probes passed | Keep the separate stat-then-unlink race as a non-blocking residual |

The principal remains the architecture/integration/red-team lane and uses `ultra` only when the user explicitly returns for review or a worker requests a cross-lane decision.

## Wave 5 accepted and active chain

| Order | Slice | Integrated or candidate chain | Status | Boundary retained |
| --- | --- | --- | --- | --- |
| 1 | Immutable dependency identity | integrated through `e0fae60` | `ACCEPTED_ON_MAIN` | Canonical lock identity is the exact Git blob and install mutation fails closed; no deployment or public-state claim follows. |
| 2 | All-World ADR-001 compiler | source `7f4806f` → `abc1ce0` → `3c50542` → `2de212d`; integrated as `bfaa9ee` → `98037c3` → `92966b9` → `de84fcb` | `ACCEPTED_ON_MAIN` for client/honour-based compilation | Receipt, retained manifest, fresh package/runtime digests, validator result, task code, support/access/source facts, and package-owned limitations are exact. Contradictory same-attempt results share identities and conflict on append. No persistence, authentication, or trusted-evidence claim follows. |
| 3 | Source authority contract/replay | source `bcae23c` → `a4920d3`; integrated as `b939ae1` → `07f5f28` | `ACCEPTED_ON_MAIN` for a pure local candidate boundary | Content-addressed policy/package identity, temporal replay, exact locators, lifecycle invalidation, and non-authority result fields passed independent review. It establishes no authenticity, rights clearance, durable human review, or publication. |
| 4 | Additive ADR-001 v2 SQL correction | source `5e5646e` + `4bbdf0d`; integrated as `eab306a` + `c708f18`; original `0689309` remains rejected evidence | `ACCEPTED_ON_MAIN` for a local SQL contract | Fresh/upgrade/golden parity, cross-version UUID/aggregate/idempotency arbitration, immutable journals/claims/outbox identity, outbox truncate refusal, and upgrade-safe bounded new error codes passed independent review and exact-main disposable replay. No hosted database, application write path, backup/restore, or production durability follows. |
| 5 | Nine-area curriculum graph | source `6040ccb` → `9b46900` → `ff08442`; integrated as `57630e6` → `7ccaad2` → `29e1aae` | `ACCEPTED_ON_MAIN` for a pure caller-asserted/untrusted contract | Content-addressed graph identity, exact binding, source correction, age/depth/access association, nine-area ordering, candidates, and gaps passed independent review. Every routable route is null; trusted adapter, curriculum sufficiency, recommendation, schedule, publication, and homeschool claims remain absent. |
| 6 | Argument & Evidence retained package | source `655665a` → `b21caaa` → `0ca2477` → `7981fe6` → `b188549` → `0a80926` → `0fdd6a5` → `a38faa1`; integrated as `d5f32d6` → `168706a` → `1a600c4` → `c0dc15b` → `7847402` → `7bff7a7` → `ceb886c` → `7ba70ef` | `ACCEPTED_ON_MAIN` as a retained unavailable internal package | The fixture is internal, JSON is the sole authored truth, runtime values are derived and frozen, exact canonical public adapters alone can attest deeply frozen public receipts, and clone/custom/forged facts fail closed. Independent reviews passed 137 focused tests and the final exact-candidate gates; integrated gates passed 626 application tests and 70 browser checks with 22 intentional skips. The browser suite covers the existing public surface, not an unavailable W5-E route. Public route, source/curriculum binding, manual AT, publication, and deployment remain forbidden or unproven. |

## Wave 6 bounded local implementation chain

The Wave 6 documents remain proposed architecture. The code below establishes reviewable local seams, not the Wave 6 adult pilot or its release gate.

| Order | Slice | Exact integrated code | Status | Boundary retained |
| --- | --- | --- | --- | --- |
| 1 | Capability map compiler | `e7f51101fc2ff273fee61d259eefb6d3af9219e0` | `ACCEPTED_ON_MAIN` for pure caller-asserted fixture validation | Validates learning intents, exact graph/review bindings, prerequisites, gaps, project/proof presence, and deterministic package identity. `assignmentAllowed` remains false. No trusted source/reviewer, persistence, publication, routing, entitlement, or learner runtime exists. |
| 2 | Governed resource candidates and disabled YouTube adapter | `50e55fecfb9e7c3cbdfd17ad77875c35ab23d206` | `ACCEPTED_ON_MAIN` for fixture-only review/eligibility and provider metadata | Exact policy/review/lifecycle identity, strict unknown-fact handling, access alternatives, bounded selection, region and Made-for-Kids gates, and deterministic fixture observations fail closed. No network, key, search/feed/transcript/media/embed, tracking, assignment, ranking, persistent reviewer, or live refresh/removal operation exists. |
| 3 | Multimodal representation registry | `9a17a32d0e37e7b7cf169de002cdb463654415f2` | `ACCEPTED_ON_MAIN` for fixture registry/frame validation | Exact package-scoped dependency and review closure, collisions, access alternatives, withdrawal replay, and the narrow deterministic authored-motion frame contract are validated. No generated visual, live runtime/publication, broad domain population, representative access study, assignment, or proof authority exists. |
| 4 | Practical project and practice compiler | `488e7210019c0cf38c303aca9775ea2b8a5a0c0f` plus strict timestamp repair `1c3a4c462de7749a062935735ddc76fc1e2c7533` | `ACCEPTED_ON_MAIN` for fixture compilation only | Revalidates exact map, capability, prerequisite, project, safety/instruction manifest, representation, milestone, critique/revision, provenance, proof, practice, and return bindings. Completion is `complete-self-declared-unverified`; all assignment, scoring, mastery, proof and external-validator authority remains false or absent. |
| 5 | Contestable exploratory-map preview | `a0eb0758fa1ac029d0ee93c4ded3129fb802f368` | `ACCEPTED_ON_MAIN` for the bounded local preview; later release remains principal-owned | Unknown questions preserve exact learner wording, display four source-verification gates, name uncompiled route elements, and accept a local unsaved revision note. The full code-cut browser matrix passed 72 / 22 intentional skips / 0 failures. The preview does not invoke the compiler, save/submit a request, retrieve a resource, create a project, publish a route, or establish reviewed curriculum. |
| 6 | Source-corroboration presentation projection | `39d86820e46fea6e7df4f4dadc48a19a5fa7e7eb` | `ACCEPTED_ON_MAIN` for the bounded presentation | Server-only exact-input projection exposes `/paths/source-corroboration` against the known runtime/public-release/project/practice/disabled-provider tuple. The sole CTA opens the existing AI & Learning World. The scratchpad is in-memory and refresh-clear; no iframe, unexpected external request, `localStorage`, evidence write, provider, assignment, proof, or return action occurs. Focused path 10/10, targeted desktop/mobile E2E 2/2, and full browser 74 pass / 22 intentional project-scoped skips / 0 fail across 96 cases in 2.3m passed. |
| 7 | Frontend and planner repair | `14b2eeb4386d61719c6cb2cfc8ec04bb28d34289` | `ACCEPTED_ON_MAIN` | Client route entry focuses `#world-content`; source-corroboration projection uses one exact practice tuple and fails closed; action contrast is 5.58:1; planner timeout/retry/race/unmount paths are bounded. |
| 8 | Adult pilot experience controller kernel | `07fd7702454151d4710df3955e22fe47c43a1c8d` | `ACCEPTED_ON_MAIN` for a non-routed pure fixture kernel | Exactly two readings, closed decision/stage pairs, exact UTC delay, proof-workspace withdrawal, opaque replay handle, canonical receipts, pollution-safe parsing, and bounded output passed independent review. No route, provider, persistence, identity, entitlement, or W6-006 completion follows. |
| 9 | Release provenance foundation | `35a1d2c5bd28c7b794b26414c6bdfe8f18097027` | `ACCEPTED_ON_MAIN`; deployed release remains blocked | Manifest, health, target policy, digest hardening, and read-only verifier passed 113 focused tests before deployment. Production then disproved the pre-build asset-digest assumption, so this foundation requires a post-build provider receipt repair. |
| 10 | Principal deployment record | deployed source `04eab4263658725d7a228c67682c40fc469757b1`; `dpl_ET6nUWvjeVMEdacWJgCbxVsCT1qn` | `DEPLOYMENT_BLOCKED` for release identity; public exact-code operation proven | READY public target at `https://modelshift.vercel.app`; exact health and emitted digest `02aef5b...`; public browser 5/5 plus pilot denial 1/1. Public verifier is 207/215 because provider metadata has CLI source and no Git source/repository tuple. Rollback, manual AT, live provider/database, and learning-validity gates remain unrecorded. |
| 11 | Provider-authenticated post-build receipt | deployed base `a3c2090` → `5783c6e` → `ed03187` → `16e048e`; local post-deploy `5c3941f` → `05f96fe` → `3d1c58c` | `ACCEPTED_ON_MAIN`; exercised against a blocked CLI candidate and the observed event schema | Only an opaque same-process capability from fixed-origin provider reads can bind a `production`/`READY` Vercel Git deployment with exact repository ID/ref/SHA and one emitted asset marker. The live nested envelope is parsed without field mixing; duplicate markers, missing nested dates, cross-deployment IDs, preview/CLI source, plain JSON, oversized responses, and caller metadata fail closed. The current candidate cannot promote because its provider Git tuple is absent. |
| 12 | Adult pilot fail-closed review shell | `694b3fd` → `59cc957` → `ccb0153` | `ACCEPTED_ON_MAIN` for fixture-only review infrastructure | Real accept/correct/reject readings, route decision, resource checkpoint, reconstruction, practice/project/critique/defence, support withdrawal, unfamiliar proof, delayed-return state, refresh reset, exact storage checks, and 320px/keyboard/access-mode tests exist. Reviewed fixture content is server-only, and ordinary builds fail if its markers reach public static assets. No entitlement, provider, evidence, cohort, or W6-006 authority follows. |

Next dependency order: authorize the Vercel GitHub App for `RanaPriyansh/modelshift`, create and verify a fresh Git-connected production candidate with the accepted provider receipt, and do not repeat CLI uploads as provenance evidence; connect no fixture contract to trusted runtime authority until source/reviewer and adult server-entitlement authority are accepted; then recruit one bounded adult cohort for a reviewed map-to-resource-to-project route with explicit support withdrawal and return. Educator operation, live provider access, minor paths, durable proof, and homeschool claims remain separate later gates.

## Wave 2 agent and review chain

| Order | Agent or review role | Exact code / review chain | Status | Boundary retained |
| --- | --- | --- | --- | --- |
| 1 | Learning Kernel implementation agent | `4dab5d9` | `ACCEPTED_ON_MAIN` as an additive v2 projector path | Pure/in-memory projection only; no route binding, persistence, identity, network, or UI change. |
| 2 | Independent adversarial review and corrective implementation | principal rejection followed by `f4179e9` | `ACCEPTED_ON_MAIN` after coherence repair | Sealed events still require journal-level coherence; no result is durable/authenticated authority. |
| 3 | Pathways implementation and boundary reviews | `48ea3fd` → `30e1dde` → `2cf81ab` | `ACCEPTED_ON_MAIN` | `/pathways` is an availability map with visible gaps, not a learner path, schedule, recommendation, entitlement, or homeschool operation. |
| 4 | Release-verifier integration and principal release gate | `b5b3170` verifier coverage; released `6e95a33`; CI `29927061567`; `dpl_CBzgm4fAVqKy2U8MVLidiTAWTTJD` | `PUBLIC_ENGINEERING_RELEASE_RECORDED` for exact source | Local/public verifier 182/182, local/public browser 61 pass / 21 intentional skips / 0 fail, and empty runtime/5xx scans prove the recorded engineering tuple only. |
| 5 | Promotion incident review | staged `dpl_BLUz...`; failed promote clone `dpl_37...`; direct production `dpl_CBzgm4fAVqKy2U8MVLidiTAWTTJD` | `RECORDED`; clone reached the alias but was not accepted as a valid release and was replaced | The clone lost build-time/digests and failed release identity. Hobby refused rollback because of one-step history; no rollback-operability claim follows. |
| 6 | Principal post-release documentation reconciliation | `docs/program` amendment after `6e95a33` | `DOCUMENTATION_ONLY` | Records the exact release and incident; this commit is not deployed and does not upgrade authority, provider, curriculum, child-cloud, or homeschool claims. |

Wave 2's dependency order placed Ratio next after the projector. Wave 3 completed that step; the current order is recorded below.

## Wave 3 agent and review chain

| Order | Agent or review role | Exact code / review chain | Status | Boundary retained |
| --- | --- | --- | --- | --- |
| 1 | Shared-runtime protocol red team | source handoffs `e151f4c` + `10d5d7e`; integrated as `8508eb8` + `285158e` | `ACCEPTED_ON_MAIN` | Runtime, not adapters, enforces trace order and blocks action-classification/support/proof bypasses. |
| 2 | Retained release-identity review | source `53f3711` + `f27dbf7`; integrated as `7ebfd87` + `e86f16c` | `ACCEPTED_ON_MAIN` | Runtime-enabled built-ins require exact canonical binding digests; legacy packages may not invent them. |
| 3 | Local evidence/access review | source `8948f66`; integrated as `193c7cc` | `ACCEPTED_ON_MAIN` | Delayed schedules require explicit valid intervals; old local v1 records remain readable and unmodified. |
| 4 | Ratio implementation agent | source `00a40a7` + `d5e0f18` + `5ce1c6c`; integrated as `a293ac3` + `8ac0e18` + `98687ea` | `ACCEPTED_ON_MAIN` after full-suite stale version/E2E rejection was corrected | Ratio uses the shared runtime, distinct test prediction, relationship-bearing proof, canonical new ID, bounded receipt, and no operational return proof. |
| 5 | Independent protocol/evidence re-review | rejected `d5e0f18`; accepted `5ce1c6c` | `ACCEPT` | A specific stale planner/E2E contract stopped integration despite otherwise green focused checks. |
| 6 | Independent access/browser review | exact source `5ce1c6c` | `ACCEPT` with manual AT named unrun | Keyboard full flow, focus/announcement, 320px deep states, table semantics, reduced motion, forced colors, proof isolation, and reset passed. |
| 7 | Principal integration and release | released `98687ea`; CI `29932913141`; `dpl_pLpQmuCP43Mc3tNt1nErf8MRH2v2` | `DEPLOYED_CANDIDATE` | Local/public verifier 182/182; public browser retained one first-request timeout that passed retry; live model and rollback rehearsal remain unrun. |

## Wave 4 agent and review chain

| Order | Agent or review role | Exact code / review chain | Status | Boundary retained |
| --- | --- | --- | --- | --- |
| 1 | Shared runtime, Force, and AI & Learning migrations | accepted integration through `c7d4011` | `ACCEPTED_ON_MAIN` | Four authored Worlds use the shared bounded runtime; domain truth remains domain-owned and receipts remain local/non-durable. |
| 2 | Independent all-World conformance and terminal journey review | 18/18 unit conformance; 8/8 terminal routed browser cases | `ACCEPT` | The matrix proves four authored flows only; manual AT, Safari, Firefox, representative learners, efficacy, and retention remain unrun. |
| 3 | Principal release and bound verification | source `c7d4011`; pre-main CI `29944088585`; exact-main CI `29944461461`; `dpl_6MUabxtnuMVzwxHf1vbM5Y1LxNfs` | `DEPLOYED_CANDIDATE` | Local/public browser 70 pass / 22 intentional skips / 0 fail, bound verifier 187/187, and empty error-level/5xx scans establish the exact engineering tuple only. |
| 4 | Rejected deployment and unbound-workflow record | rejected `dpl_4eY4F3vMV1hado7TG1BZN11Mgzvt`; unbound workflow `29945127093` | `RECORDED` | The rejected deployment had unknown build time/digests and was replaced. The unbound workflow is `DEPLOYMENT_BLOCKED` at 184/187 for a lock-digest reproducibility mismatch (`12f19d...` committed/public vs `f43d90...` post-install) and empty retained metadata; it is not a product/public verifier failure. |

The next dependency order is additive v2 SQL/golden-fixture review before persistence; durable source authority; broader reviewed curriculum/path graphs; manual assistive-technology review; lock-digest reproducibility; and separately authorized rollback/alerting work. Cloud identity, provider enablement, certification, and homeschool operation remain separate authorization tracks.

## Packet A — Experience and design system

**North Star:** one calm, question-led instrument from intake to bounded evidence at 320 px through desktop.

Owned paths for the next slice:

- `app/forge-system.css` or an explicitly approved replacement;
- reusable presentation primitives under `src/components/forge/`;
- focused visual/accessibility tests;
- a worker-owned fidelity report outside `docs/program/`.

Required work:

1. start a new branch from current `origin/main`; do not rebase/cherry-pick the old commit wholesale;
2. compare the old seven-file slice to current Studio/auth/primary-source changes;
3. port only non-conflicting tokens and primitives;
4. unify typography/paper/instrument surfaces without changing World logic;
5. cover unique accessible names, roving tabs where present, 44 px targets, 16 px mobile inputs, focus, forced colors, contrast, and reduced motion;
6. test home, Studio, login/account, all four Worlds, evidence, and trail.

Stop gate: one clean commit; no route behavior regression; exact visual/browser evidence; a conflict/disposition list for anything intentionally not ported.

Principal disposition on 2026-07-22: **accepted and integrated**. The first handoff was held until rendered contrast, forced-colors, 44 px link targets, the quiet-status contrast defect, and the fidelity-report ownership/404 wording were corrected in an additive commit. Two read-only reviews accepted the resulting chain. On integrated code SHA `f7a3bc4`, lint, typecheck, 182 application tests, 9 evaluator tests, optimized build, and the production-mode Chromium matrix passed; browser result was 45 passed, 19 intentional cross-project skips, 0 failed. This closes Packet A's bounded presentation/access slice, not screen-reader testing, complete nonvisual representation equivalence, or the broader FORGE objective.

## Packet B — Trust, auth, and private evidence

**North Star:** adult continuity and private sync can be enabled without granting false age/guardian authority or weakening learner data ownership.

Owned paths:

- `src/lib/forge-auth/**`, future private-sync module, focused account/actions/routes;
- additive Supabase migration/tests for private evidence and consent;
- auth/privacy E2E and worker documentation.

Required work:

1. branch from current main and audit `24edb6f` against the new nonce CSP, cookie headers, key rejection, typed event spine, and device profiles;
2. choose compatible environment naming; no `NEXT_PUBLIC_*` secret and no service role;
3. preserve adult-entry self-attestation as non-authoritative;
4. add CAPTCHA/application limiter integration boundary before any configured-cloud release;
5. make sync explicit per record/batch, idempotent, size bounded, schema validated, RLS owner-only, and separately deletable;
6. test two-account isolation, token refresh, signout, duplicate sync, partial failure, local/cloud deletion, consent revocation, and under-18 refusal in a disposable project or emulator.

Stop gate: staging/code packet only unless the user separately approves a Supabase organization, displayed cost, and live test project.

Principal disposition on 2026-07-22: **accepted and integrated as a fail-closed staging boundary**. The handoff was held until URL audience hints could not bypass a valid device profile, corrupt or absent profiles failed closed, direct child-capable World routes required the local selection/grown-up gate, browser-controlled age or adult self-attestation could not create cloud authority, profile/age mutation privileges were revoked, and an actual legacy migration path preserved immutable retired consent history. A database trigger now rejects new `private_evidence_persistence` consent even for the local `service_role`/BYPASSRLS path; both authenticated and elevated refusals were executed against disposable PostgreSQL databases. Two read-only reviews accepted the final source and current-base integration chains. On integrated code SHA `9a4068f`, lint, typecheck, 249 application tests, 9 evaluator tests, optimized build, fresh and legacy-upgrade SQL fixtures, and the complete production Chromium matrix passed; browser result was 55 passed, 19 intentional cross-project skips, 0 failed. Cloud auth, private-evidence sync, and live account continuity remain structurally disabled—not production-operable.

## Packet C — Homeschool pathways and entitlement

**North Star:** broad foundations and learner-chosen depth coexist without grade-level lockstep, hidden recommendation scores, or certification fiction.

Owned paths:

- `src/forge/pathways/**`;
- pathway contract/tests and a worker-owned architecture note;
- minimal registry integration only after contract review.

Required work:

1. port the pure module from `dd6fe0e` onto current main;
2. reconcile capability IDs, age modes, evidence tiers, source policies, and event vocabulary;
3. produce deterministic issues for breadth, choice, access, consent/assent, relationships, protection, and portability;
4. add fixtures for child-with-grown-up, teen, adult, disability/access alternatives, missing foundation, coercive sequence, hidden gamification, and invalid evidence claims;
5. return only `needs-evidence` or `evidence-complete-for-independent-review`.

Stop gate: no UI scheduler, jurisdictional compliance claim, accreditation, attendance record, or “homeschool ready” wording.

Principal disposition on 2026-07-22: **accepted and integrated**. The handoff was held through three red-team rounds until production evaluation could not promote fixture-injected evidence, the published catalog remained `needs-evidence` without a Packet E trusted receipt, under-18 open-web actions failed closed, positive homeschool/mastery/retention/transfer claims could not hide behind denial prefixes, and honest bounded limitation lists remained expressible. A read-only reviewer accepted the final whole-statement grammar after a 14-case adversarial matrix. On integrated code SHA `033bb9a`, lint, typecheck, 57 focused pathway tests, 239 application tests, 9 evaluator tests, and the optimized build passed. This accepts a pure deterministic review contract only; no curriculum entitlement, jurisdictional operation, attendance record, accreditation, or homeschool-readiness claim is established.

## Packet D — Release, evaluation, and observability

**North Star:** every public URL identifies its exact source and can be rolled back without confusing engineering health with educational validity.

Owned paths:

- `.github/workflows/**`, release verification scripts, health endpoint;
- `docs/operations/**`, generated/sanitized report paths;
- focused tests for release identity and verifier allowlists.

Required work:

1. branch from current main and disposition `a98f8bb` file by file;
2. diagnose `dpl_GwTK18jVR2NmEb4VKa3KGhf6hLzi` BLOCKED using build/deployment evidence;
3. verify local Git author configuration and Vercel membership rules without rewriting shared history;
4. produce a candidate endpoint/report containing app name, source SHA, build time, runtime mode, cloud/provider flags without secrets;
5. add immutable CI for lint/typecheck/unit/eval/build/E2E and artifact retention;
6. add production verifier checks for four Worlds, Studio, device profile, CSP nonce, no-cloud state, and release SHA;
7. never deploy from the worker.

Stop gate: exact diagnosis, exact remediation, one clean commit, no deployment capability in CI without a separate approval environment.

Principal disposition on 2026-07-22: **accepted and integrated**. The stale worker ancestry was not merged. Packet D was replayed linearly onto the exact accepted A/B/C base and held through independent red-team review until state/outcome authority, artifact selection, non-overwrite/symlink/path safety, exact schemas, SSRF/rebinding/redirect defenses, and a 1,260-case malformed-input matrix passed. On replay SHA `1c5dd3a`, 373 application tests, 9 evaluator tests, lint, typecheck, optimized build, 165 local verifier checks, and 55 production Chromium checks passed; 19 project-scoped duplicates were intentionally skipped. This accepts the release machinery, not a provider-quality, database-operation, educational-validity, or public-deployment claim.

Post-release record: source `6e95a33c4cd82e2b7529f3b5980766a7c13ed068` is public at `https://modelshift.vercel.app` through production deployment `dpl_CBzgm4fAVqKy2U8MVLidiTAWTTJD` and immutable URL `https://forge-learning-ffh45hv8c-ranapriyanshs-projects.vercel.app`. CI run `29927061567` passed; local/public verifier was 182/182; local/public optimized browser was 61 pass / 21 intentional skips / 0 fail; 431 application and 13 evaluator tests passed; live model was NOT RUN; runtime-error and exact-deployment 5xx scans were empty. The staged `dpl_BLUz...` had exact health, but promoted clone `dpl_37...` lost build-time/digests and failed release identity. Hobby refused rollback to the prior known-good deployment because only one-step history was available. Direct production replaced it. Rollback artifacts `dpl_DcKE...` (`cd418b8`) and `dpl_HPts...` (`79053`) are READY, but the CLI target attempt failed, so rollback rehearsal is `NOT_EVALUATED`. This is an exact engineering-release record, not a claim of rollback operability, durable authority, live provider quality, cloud auth, broad curriculum, accessibility-session completion, efficacy, homeschool readiness, or certification.

Wave 3 release amendment: source `98687eaac20f956698000467cd01ea1da9b7fd6e` is public at the same alias through `dpl_pLpQmuCP43Mc3tNt1nErf8MRH2v2` and immutable URL `https://forge-learning-a3jw66bda-ranapriyanshs-projects.vercel.app`. CI `29932913141`, 465 application tests, 13 evaluator-contract tests, 182/182 local verifier, 61 local browser passes with 21 intentional skips, deep Ratio 320px/access review, and final bound public verifier 182/182 passed. Public browser exited passing but retained one first-request mobile API timeout that passed retry; focused repeats produced two direct passes and one first-attempt timeout that passed retry. Deployment error and 5xx scans were empty. Live model and rollback rehearsal were NOT RUN, so release state remains `DEPLOYED_CANDIDATE`.

## Packet E — Learning Kernel and World Factory

**North Star:** new subjects plug into one trusted learning protocol while preserving domain-specific truth.

Owned paths:

- new `src/forge/world-runtime/**` and conformance tests;
- thin adapters in one selected World;
- no global styling/auth/data changes.

Required work:

1. extract interfaces from the four current Worlds rather than designing an abstract curriculum framework first;
2. define stable stages, actions, side effects, support events, proof conditions, evidence emission, focus/motion/access hooks, and validator contract;
3. define domain plugin interfaces without forcing physics concepts onto source or mathematical reasoning;
4. build a package linter and exhaustive invalid-manifest fixtures;
5. migrate Primary Source Reasoning first because it stresses non-STEM semantics and authentic sources;
6. prove behavior equivalence with reducer, component, and browser tests.

Stop gate: no second source of truth, no model-graded proof, no generic mastery score, and no edits to the other three Worlds in the first slice.

Principal disposition on 2026-07-22: **accepted and integrated as a one-World runtime slice**. Independent review confirmed strict bound-versus-legacy source provenance, domain-owned validator projection, preserved rejected reducer state, real disagreement/test-prediction trace, typed construct-preserving accommodations, declared runtime actions, strict-CSP operation, and an explicitly local/non-durable receipt. Primary Source Reasoning and its retained content/runtime identity are `1.0.1`; protocol, validator, source, and proof-lock changes alter the retained runtime-binding digest while release health continues to use the exact manifest-file SHA-256. Exact replay HEAD `1cde10d` passed 400 tests, lint, typecheck, build, 166 local release checks, and production Chromium/320 px/keyboard/reduced-motion/text-alternative checks. Packet C remains `needs-evidence`: this local receipt is not a trusted or persisted evidence record, and the other three Worlds are not yet migrated to the shared runtime.

Wave 3 amendment: **Ratio is accepted and integrated as the second shared-runtime World** on released source `98687ea`. The accepted implementation adds a distinct post-compiler separating-test prediction, a domain-owned exact-answer-plus-relationship criterion, runtime-only component dispatch, proof support/model/replay blocking with typed access preserved, canonical new capability IDs with legacy read compatibility, one non-durable local receipt, and explicit absence of delayed scheduling. The shared runtime and retained release contracts were independently hardened before the migration landed. Force and Motion and AI & Learning remain unmigrated; neither receipt is durable, authenticated, projector-bound, or trusted pathway evidence.

## Packet F — AI Lesson Intelligence

**North Star:** models compete on explanation and lesson-design quality while deterministic policy and human review own truth, publication, and proof.

Owned paths:

- `src/lib/lesson-studio/**`, Studio API/UI extensions, provider eval fixtures;
- new review-workflow module that remains local/staged;
- no published World mutation in the first slice.

Required work:

1. verify current structured-output contracts and default model IDs against official provider docs;
2. add provider capability metadata, model override validation, time/token/cost budgets, rate-limit/refusal classification, and request correlation without learner text logging;
3. split generation from critique/source-plan/revision; no model self-approval;
4. define review states: draft, source-needed, factual-review, pedagogy-review, access-review, proof-review, approved-package, rejected, withdrawn;
5. create cross-domain/age golden fixtures and metrics for two-reading quality, disagreement clarity, separating-test validity, source need completeness, answer leakage, cold-transfer validity, safety, latency, and cost;
6. keep live suites credential-gated, redacted, and provider-specific.

Stop gate: mocked/deterministic test coverage plus a live-eval harness; no managed key enablement, live cost, or publication without separate authority.

Principal disposition on 2026-07-22: **accepted and integrated as a structurally locked draft/review slice**. The handoff was held through two independent review rounds until the mandatory safety stage, scoped reviewer roles, exact immutable refs, complete multi-source ADR-003 receipt, closed/redacted live-eval outcomes, OpenAI refusal/incomplete parsing, adult server-authority-before-body boundary, and retired managed-key surface all passed. A final forged-state repro then required every persisted review projection to equal deterministic replay from its immutable draft seed and ordered decisions; shortened, reordered, fabricated-final, source-ref, publication, grade, and supersession attempts fail closed. Exact replay HEAD `fa634ea` passed 392 application tests, 13 evaluator tests, lint, typecheck, build, and local rendered Studio checks. No live provider call, credential, spend, durable review, source attestation, publication, proof grade, or public connector enablement is claimed.

## Handoff format required from every lane

```text
STATUS: READY_FOR_PRINCIPAL_REVIEW | NEEDS_DECISION | BLOCKED
BASE SHA / BRANCH / HANDOFF SHA / WORKTREE
GOAL AND NORTH STAR
FILES CHANGED AND OWNERSHIP DEVIATIONS
BEHAVIOR DELIVERED
CONTRACTS ADDED OR CHANGED
TESTS: exact commands, counts, skips, environment
SECURITY / PRIVACY / ACCESS / CLAIM REVIEW
CONFLICTS WITH CURRENT MAIN OR OTHER LANES
EXTERNAL ACTIONS: must be none unless separately authorized
DECISIONS NEEDED
KNOWN RISKS AND ROLLBACK
NEXT BOUNDED GOAL
```
