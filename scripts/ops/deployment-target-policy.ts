/** Compatibility re-export for release scripts. The authoritative policy is
 * shared with the health-manifest builder, so build/runtime and verifier use
 * the same checked-in target facts. */
export * from "../../src/operations/deployment-target-policy";
